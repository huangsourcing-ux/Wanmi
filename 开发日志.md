# Wanmi.AI 开发日志

本文件按开发阶段和日期记录已完成工作、验证证据、遗留门槛与 Git 状态。产品范围、架构、安全和上线门槛仍以上级批准文档为准；历史失败通过后续条目修正，不回写成未发生。

## D0：Payload 架构验证

### 2026-08-03

- 阶段状态：进行中。
- 完成内容：建立 pnpm workspace、Next.js/Payload Web 与 Admin、PostgreSQL/Who-Dat/MinIO 本地环境、全部最小 Collections 与 RBAC、管理员 TOTP、customer OTP/opaque Session、三类 Payload Jobs、commerce 幂等、provider mocks、迁移与类型漂移门禁、CI、ADR 和 Runbook。
- 验证证据：25 个 Vitest 单元测试、5 个 PostgreSQL/MinIO 集成测试、2 个 Playwright 场景、生产构建、linux/amd64 同镜像构建和 Gitleaks 通过；本地 Web/Worker 可独立启动。
- 遗留门槛：真实 OSS、隔离 RDS migration、同 VPC Worker 恢复、2 vCPU/4 GiB ECS 内存/重启/重建/RTO 尚未验证。
- Git 状态：工作分支 `codex/d0-payload-baseline`，尚未提交。

### 2026-08-04

- 阶段状态：条件通过，获准进入 D1。
- 完成内容：创建上海私有 D0 OSS Bucket；完成公共 `storage-s3` 与私有 `ali-oss` 真实路径验证；在隔离 RDS 数据库执行初始 migration 和二次无变更验证；记录 SSH 隧道长任务限制及 processing lock 恢复；将 Undici 精确覆盖到 7.29.0；更新 D0 证据、资源清单、ADR、Runbook 和冻结基线。
- 验证证据：真实 OSS 上传、读取、ETag、60 秒签名、删除和清理通过；现有 RDS 业务库未修改；`make check`、25 个单元测试、5 个本地集成测试、2 个 Playwright 场景、生产/同镜像构建、依赖 high 门禁和 Gitleaks 通过。
- 遗留门槛：共享 ECS 仍承载其他项目；2 vCPU/4 GiB 生产 Linux 内存、Web/Worker 独立重启、同 VPC commerce Job 中断恢复、空节点重建和两小时 RTO 已转入 D7，继续阻塞开发整体验收与生产上线。RDS SSL/公网入口需在生产前收敛，本轮披露的云凭据必须轮换。
- Git 状态：工作分支 `codex/d0-payload-baseline`；D0 基线提交 `c7449b2` 已推送，批准标签 `p1-docs-approved-2026-08-04-1` 已发布；GitHub Draft PR #1 已创建并交由 Claude 审核。

## D1：公共站与管理基础

### 2026-08-04

- 阶段状态：进行中；D1-01 主站骨架与首页入口完成。
- 完成内容：建立可信浅色视觉系统、响应式主导航与移动端 Sheet、页脚、无障碍跳转、首页域名/关键词 GET 入口、六类工具入口、内容/TLD/专题入口、空赞助位、数据来源与服务边界说明；建立工具、价格、内容、专题、帮助和四类合规说明的可达路由骨架。首页并行读取 Payload 已启用导航和已发布内容，固定 `overrideAccess: false`，过滤外链/禁用项并按栏目独立回退；未调用真实 provider、未保存查询输入、未新增 API 或数据库迁移。
- 验证证据：`make check` 通过：29 个 Vitest 单元测试、9 个 PostgreSQL/MinIO 集成测试、Next.js 生产构建、linux/amd64 同镜像构建、依赖 high 门禁和 Gitleaks 均通过；`make test-e2e` 的 5 个 Playwright 场景与桌面/390px 手机截图检查通过。查询参数页验证 `noindex, nofollow`，未知工具/合规 slug 验证 404。
- 遗留风险/门槛：通用结果与错误状态、完整 canonical/robots/sitemap、SEO/Redirects 深化、Wanmi.ai/www Nginx 跳转、管理员与审计基础仍属于后续 D1；合规页面只是待专业复核的开发期边界说明；注册、支付、履约和生产上线继续关闭。
- Git 状态：工作分支 `codex/d1-public-foundation`；D1-01 实现提交 `bd1904e` 已推送；GitHub Draft PR #2 已创建，待 Claude 审核。

### 2026-08-04（D1-02）

- 阶段状态：进行中；D1-02 通用状态与错误契约完成。
- 完成内容：建立共享 Zod `Result<T>` 六状态契约、兼容 RFC 9457 的 Problem Details、请求 ID 集中校验、客户端安全解析和 provider 结果脱敏映射；错误响应保留 `code/message/traceId` 并增加 `type/title/status/detail/retryable/action`，成功响应保持原有 body。接入可访问表单字段与首页/查询页复用表单、空态/部分成功/失败/限流/降级面板、请求 ID 导航级上下文、内容栏目 Skeleton、公共错误边界和品牌化 404。根级 Loading 曾使动态 `notFound()` 流式返回 200，已将 Loading 下沉到文章、专题和价格栏目并恢复真实 404；未调用真实 provider，未修改 Payload schema、迁移或生成类型。
- 验证证据：`make check` 通过：41 个 Vitest 单元测试、9 个 PostgreSQL/MinIO 集成测试、Next.js 生产构建、linux/amd64 同镜像构建、依赖 high 门禁和 Gitleaks 均通过；`make test-e2e` 的 6 个 Playwright 场景通过，覆盖 RFC 错误体、合法/非法请求 ID、复用查询表单、内容空态、移动端品牌化 404 和真实 404 状态。桌面内容空态、390px 404 与查询页截图检查无溢出。
- 遗留风险/门槛：Result 的部分成功和限流状态已通过组件测试，真实工具数据映射仍待 D2；canonical/robots/sitemap、SEO/Redirects、Wanmi.ai/www 跳转、管理员与审计基础仍属于后续 D1；注册、支付、履约、生产部署和真实外部写操作继续关闭。
- Git 状态：工作分支 `codex/d1-result-states`；D1-02 实现提交 `7035b36` 已推送；GitHub PR #3 已创建并指向 `main`，待 Claude 审核，未批准或合并。

### 2026-08-04（D1-03）

- 阶段状态：进行中；D1-03 SEO 基础与 Payload SEO 共享能力完成。
- 完成内容：建立以 `NEXT_PUBLIC_SERVER_URL` origin 为唯一来源的绝对 canonical、路由级 Open Graph/Twitter、工具查询参数页 `noindex, nofollow`、`robots.txt`、静态 sitemap 和 1200×630 品牌分享图；sitemap 只包含当前真实可访问的稳定页面，不提前加入 D3 内容详情。扩展官方 `@payloadcms/plugin-seo` 的共享 `WanmiSeoMeta`，新增同源 canonical、默认关闭的 `noIndex` 与文章/专题/TLD 搜索预览 URL；生成 Payload migration、snapshot 和类型，公开读取继续固定 `overrideAccess: false`，草稿 SEO 数据不可见。
- 验证证据：生成文件与 migration 漂移检查、lint、TypeScript strict、46 个 Vitest 单元测试、9 个 PostgreSQL/MinIO 集成测试、8 个 Playwright 场景、Next.js 生产构建、linux/amd64 同镜像构建、依赖 high 门禁和 Gitleaks 均通过；分享图实测为 1200×630 PNG。隔离空库顺序应用全部 migration 后得到 12 个新增 SEO 列；模拟现有 2 条 migration 基线后单独升级 D1-03 migration，同样得到 3 条 migration 记录和 12 个新增列，隔离数据库已删除。
- 遗留风险/门槛：动态文章、专题和 TLD 详情、动态 sitemap、草稿实时预览与完整内容 SEO 集成仍属于 D3；Redirects、Wanmi.ai/www Nginx 跳转、管理员/RBAC、审计和第一方事件仍属于后续 D1；生产必须将公开 origin 配置为 `https://wanmi.net`。依赖审计保留现有 2 个低危和 2 个中危，无 high/critical；注册、支付、履约、生产部署和真实外部写操作继续关闭。
- Git 状态：工作分支 `codex/d1-seo-foundation`；D1-03 实现提交 `d48f5f3` 已推送；GitHub PR #4 已创建并指向 `main`，待 Claude 审核，未批准或合并。

### 2026-08-04（D1-04）

- 阶段状态：进行中；D1-04 受控重定向与 canonical 主机跳转完成。
- 完成内容：将 Payload Redirects 收敛为永久 301；自定义目标只接受规范化站内路径，引用目标只接受已发布文章、专题和 TLD 页面，拒绝保留路径、开放跳转、直接/多跳循环和超过 10 跳的链路。`content_editor`/`system_admin` 可创建更新，仅 `system_admin` 可删除；创建、更新、删除通过同一 `req` 写入脱敏审计。公共 GET/HEAD Proxy 使用 `overrideAccess: false` 分页加载规则、折叠最终目标并保留查询参数和请求 ID，采用每进程 30 秒缓存、并发刷新去重、stale-on-error 与冷启动失败放行。新增遗留 302 先规范化再收窄 enum 的 migration、snapshot 和生成类型；新增 Wanmi.ai/www 与 canonical HTTP 的纯 301 Nginx 配置，别名不含 `proxy_pass`。
- 验证证据：`make check` 通过生成/迁移漂移、空库完整 migration、D1-03 遗留 302 升级、固定 digest Nginx `nginx -t`、4 个 HTTP/3 个 HTTPS 别名跳转、lint、TypeScript strict、65 个单元测试、12 个 PostgreSQL 集成测试、Next.js 生产构建、linux/amd64 镜像构建、依赖 high 门禁和 Gitleaks；`make test-e2e` 的 9 个 Playwright 场景通过，数据库规则真实返回 301、最终 canonical 目标、原查询参数和请求 ID，未知路径保持 404。依赖审计仍为既有 2 个低危和 2 个中危，无 high/critical。
- 遗留风险/门槛：Nginx 配置尚未部署，生产证书必须覆盖 `wanmi.ai`、`www.wanmi.ai` 和 `www.wanmi.net`；本切片未修改共享 ECS、未申请证书或切换流量。规则按进程最多 30 秒传播，动态内容详情与完整 Redirects 运营仍随 D3；管理员/TOTP/RBAC 深化、通用审计后台和第一方事件仍属于后续 D1。注册、支付、履约、真实 provider 写操作和生产上线继续关闭。
- Git 状态：工作分支 `codex/d1-redirect-foundation`；D1-04 实现提交 `371a965` 已推送；GitHub PR #5 已创建并指向 `main`，待 Claude 审核，未批准或合并。

### 2026-08-04（D1-04 合并后 CI 修正）

- 阶段状态：D1-04 已合并；合并后的 `main` push CI 冷启动失败已定位并完成修正。
- 完成内容：GitHub Actions 冷启动时，`docker compose up -d postgres` 在 PostgreSQL healthcheck 通过前执行 `createdb`；迁移等待修正后又暴露 Nginx 容器首次请求发生 `ECONNRESET`。迁移门槛现使用 Compose `--wait --wait-timeout 60`，Nginx 七入口验证在最多 5 秒的有界窗口内等待容器就绪；持续配置错误仍在窗口结束后失败，不改变产品、schema 或部署边界。
- 验证证据：本地先停止 PostgreSQL 再运行 `make verify-migrations`，容器从 Starting/Waiting 到 Healthy 后，空库和遗留 302 升级验证通过；Nginx 固定 digest 的语法和七入口验证连续两次通过。Hotfix 提交 `3309d95` 的 GitHub push 与 pull_request 两轮完整 CI 分别在 7 分 08 秒和 7 分 12 秒通过，均越过原来 1～2 分钟内失败的冷启动阶段。
- 遗留风险/门槛：该修正只处理 CI 冷启动确定性，不代表 Nginx 已部署；共享 ECS、证书、生产流量和真实 provider 仍未修改。合并后仍须确认 `main` push CI 通过，才视为本次 CI 修正闭环。
- Git 状态：工作分支 `codex/d1-redirect-ci-hotfix`；修正提交 `fe2d7dc`、`3309d95` 已推送；GitHub PR #6 两轮检查通过，按项目负责人指令等待最终合并。

### 2026-08-04（D1-05）

- 阶段状态：进行中；D1-05 管理员认证、MFA 与 Session 生产化完成。
- 完成内容：将独立 `admins` Auth Collection 收敛为 active/disabled、固定 12 小时且关闭自动刷新的 Payload Session；强制 14～128 字符密码与 SHA-1/6 位/30 秒/±1 窗口 TOTP，将 MFA 密文、一次性恢复码哈希、重放时间步、CAS 版本和锁定状态迁入拒绝通用 API 的隐藏凭据 Collection。新增 24 小时、256-bit、数据库仅存 HMAC 的新管理员/MFA 重置邀请，token 只在 fragment 返回一次；受邀人自行设密码、扫码并验证 TOTP，8 个 72-bit 恢复码仅展示一次。新增安全化 Session 查询与本人/全部/远程撤销、最后 active `system_admin` 服务层与数据库保护、身份变更即时撤销、脱敏审计、统一 `/api/v1` 登录/邀请/Session 契约、绑定 QR 和最小账号安全后台；关闭 GraphQL 及 Payload 默认登录、首用户注册、忘记/重置密码、解锁和 refresh-token 入口。升级 migration 无损搬迁 D0 管理员 MFA 并回填 active；安全扫描器跳过索引中已在工作区删除的路径，避免 GraphQL 路由删除导致 Gitleaks 前置复制失败。
- 验证证据：`make check` 通过生成类型/import map 与 schema 漂移、空库完整 migration、D1-03 遗留 Redirect、D1-05 遗留管理员 MFA、最后系统管理员并发约束、Nginx、lint、TypeScript strict、81 个单元测试、17 个 PostgreSQL 集成测试、Next.js 生产构建、linux/amd64 同镜像构建、依赖 high 门禁和 Gitleaks；`make test-e2e` 的 12 个 Playwright 场景通过，覆盖密码 + TOTP、fragment QR、恢复码一次展示、受保护后台、禁用账号、关闭的认证/GraphQL 入口及 token 不进入请求 URL、控制台或 Web Storage。依赖审计仍为既有 2 个低危和 2 个中危，无 high/critical。
- 遗留风险/门槛：本切片仅完成管理员身份层 RBAC；内容、广告、分析和系统业务 Collection 的完整权限矩阵、通用审计后台导航及第一方事件入口仍属于 D1-06/D1-07。邀请继续由 `system_admin` 通过受控渠道手工发送；未接入邮件/短信，未部署、未修改共享 ECS，注册、支付、履约和真实 provider 写操作继续关闭。
- Git 状态：工作分支 `codex/d1-admin-auth`；D1-05 实现提交 `8b2156b` 已推送；GitHub Draft PR #7 已创建并指向 `main`，待 Claude 审核，未批准或合并。

### 2026-08-05（D1-05 安全审核修正）

- 阶段状态：D1-05 审核问题已确认并完成双层修正，PR 继续等待复核。
- 完成内容：复现 Next.js Proxy 对禁用 Payload 认证路径的 percent-encoding 绕过，并确认 `forgot-password` 可在未认证时生成管理员重置 token。入口现统一解码和规范化 pathname 后再匹配禁用路径，并对畸形敏感路径失败关闭；`admins` Collection 新增 `beforeOperation` 后备保护，在 Payload 写入前拒绝默认 login、forgot/reset-password、unlock 和 refresh，仅允许携带服务端 MFA 上下文的统一 `/api/v1` 登录服务。未修改 Nginx、schema、migration 或生成类型。
- 验证证据：单元测试覆盖单层/双层编码、编码斜杠、替代分隔符、畸形转义、编码 GraphQL 和必要管理员 REST 放行；PostgreSQL 集成测试确认 `forgotPassword` 被拒绝且 reset token/expiration 保持不变；Playwright 通过真实 Next.js HTTP 链路确认编码变体均返回 404。`make check` 通过生成/迁移漂移、空库和遗留升级、Nginx、lint、TypeScript strict、92 个单元测试、18 个 PostgreSQL 集成测试、生产构建、linux/amd64 镜像、依赖 high 门禁和 Gitleaks；`make test-e2e` 的 12 个场景通过。依赖审计仍为既有 2 个低危和 2 个中危，无 high/critical。
- 遗留风险/门槛：该修正确保应用入口与 Payload Collection 双层拒绝，不依赖生产 Nginx 是否保留编码 URI；业务 Collection 完整角色矩阵、通用审计后台和第一方事件仍属于 D1-06/D1-07。未部署、未修改共享 ECS，注册、支付、履约和真实 provider 写操作继续关闭。
- Git 状态：工作分支 `codex/d1-admin-auth`；安全修正提交 `22f3638` 已推送至 GitHub Draft PR #7，push 与 pull_request 两套 CI 均通过，待 Claude 复核，未批准或合并。

### 2026-08-05（D1-06）

- 阶段状态：进行中；D1-06 管理员角色边界完成，D1-07 审计公共组件与后台导航骨架、D1-08 第一方事件入口待后续切片。
- 完成内容：建立覆盖匿名、customer、禁用管理员及四类 active 管理员的完整 Payload Collection 权限与后台可见性矩阵；内容编辑与广告运营写权限隔离，广告运营读取完整反馈和本人范围审计，分析角色只读脱敏广告参考、反馈摘要及聚合/对账，系统管理员获得全局读取但实名、价格、commerce、provider、对账和人工复核的通用写入口保持关闭。客户业务数据继续按本人行级约束，并隐藏上游成本、规则/报价快照、订单内部证据和操作者标识；Redirects、Forms 和 Form Submissions 同步角色边界。共享 Media 仍只由内容角色管理，广告媒体分类留到 D3。
- 验证证据：`make check` 通过生成类型/schema 漂移、空库和 D1-03 Redirect/D1-05 管理员 MFA 遗留升级、最后系统管理员约束、Nginx、lint、TypeScript strict、144 个单元测试、23 个 PostgreSQL 集成测试、生产构建、linux/amd64 同镜像、依赖 high 门禁和 Gitleaks；`make test-e2e` 的 15 个 Playwright 场景通过，覆盖内容编辑、广告运营和分析角色的后台导航、未授权 Collection 路由 404 与 REST 越权写入 403。依赖审计仍为既有 2 个低危和 2 个中危，无 high/critical。
- 遗留风险/门槛：本切片不新增 schema、migration 或公开 API；未实现 D1-07/D1-08，也未提前实现 D3 广告媒体分类、聚合报表或自定义后台。未部署、未修改共享 ECS，注册、支付、履约和真实 provider 写操作继续关闭。
- Git 状态：工作分支 `codex/d1-role-boundaries`；D1-06 实现提交 `0e18785` 已推送；GitHub PR #8 已创建并指向 `main`，待 Claude 审核，未批准或合并。

### 2026-08-05（D1-07）

- 阶段状态：进行中；D1-07 审计公共组件与后台导航骨架完成，D1-08 第一方事件入口待后续切片。
- 完成内容：建立唯一强类型审计写入服务和兼容现有标识的事件目录，统一 action、actor 与 `targetType`；Redirect、管理员账号/邀请、TOTP/MFA、登录、Session 撤销及系统 Local API 均传递同一 `req` 写入，metadata 在入库前递归脱敏，Session 只记录 `sessionIdHash`。Payload Admin 按内容、广告、身份、实名、交易、履约、运营七域分组，保留 D1-06 服务端权限；审计列表按时间倒序并支持安全搜索，`system_admin` 全量、`ad_operator` 仅本人 admin 事件且 metadata 隐藏、`analyst` 无权限。新增命名 migration 和 `actorType + actorId + createdAt` 复合索引，不改写历史审计。
- 验证证据：`make check` 通过生成类型/schema 漂移、空库和 D1-03 Redirect/D1-05 管理员 MFA 遗留升级、D1-07 审计索引、最后系统管理员约束、Nginx、lint、TypeScript strict、154 个单元测试、24 个 PostgreSQL/MinIO 集成测试、生产构建、linux/amd64 同镜像、依赖 high 门禁和 Gitleaks；`make test-e2e` 的 15 个 Playwright 场景通过，覆盖 system_admin 七域导航、内容/广告/分析角色分组、广告运营本人审计过滤、分析角色审计 404 与既有越权写入保护。依赖审计仍为既有 2 个低危和 2 个中危，无 high/critical。
- 遗留风险/门槛：本切片不实现 D1-08 第一方事件采集，不修改订单状态机、支付或 provider 写接口；未部署、未修改共享 ECS，注册、支付、履约和真实 provider 写操作继续关闭。
- Git 状态：工作分支 `codex/d1-07-audit-admin-navigation`；D1-07 实现提交 `42ac721` 已推送；GitHub Draft PR #9 已创建并指向 `main`，待 Claude 审核，未批准或合并；用户未跟踪的 `docs/planning/` 保持未修改且未进入提交。

### 2026-08-05（D1-08）

- 阶段状态：D1 完成；D1-08 第一方事件入口、隐私边界和前端最小采集完成。
- 完成内容：新增严格 Zod 判别联合保护的 `POST /api/v1/events` 与 `firstPartyEvents` Collection，只接受页面/来源/设备类别、固定工具、输入类别、无点单标签 TLD、标准结果、数据源和耗时桶等聚合维度；客户端自动采集 `page_viewed` 与 `tool_submitted`，完整查询只在表单本地用于推导输入类型和 TLD，`wanmi.net` 的事件体仅含 `tld: net`。请求固定 `credentials: omit`、origin-only referrer 和 best-effort keepalive，不设置分析 Cookie、localStorage、sessionStorage 或持久客户端 ID；DNT/GPC 在客户端不发请求、服务端返回 204 且不写入。将 D1-07 递归敏感值清洗抽成共享隐私组件，审计行为保持兼容，事件写入再次复用同一规则。原始事件的通用 create/update/delete 全关，仅受控 service 写入、`system_admin` 读取，`analyst` 无入口和直接读取权限；每分钟 1000 条全局限流不生成用户标识。新增 migration、snapshot、生成类型及事件/工具时间复合索引。
- 验证证据：`make check` 通过生成类型/import map/schema 漂移、空库完整 migration、D1-03 Redirect/D1-05 管理员 MFA 遗留升级、最后系统管理员约束、D1-07 审计索引、D1-08 表结构禁止列检查和 migration down/up 往返、Nginx、lint、TypeScript strict、167 个单元测试、26 个 PostgreSQL/MinIO 集成测试、Next.js 生产构建、linux/amd64 同镜像构建、依赖 high 门禁和 Gitleaks；`make test-e2e` 的 16 个 Playwright 场景通过，验证完整查询域名不进入事件体、请求不带 Cookie、referrer 不带查询参数、DNT/GPC 停止采集、事件 503 不阻塞查询，以及 analyst 后台隐藏和直接路由 404。依赖审计仍为既有 2 个低危和 2 个中危，无 high/critical。
- 遗留风险/门槛：D2 接入 `tool_completed`/`tool_failed` 实际发送；D3 完成广告事件、后台聚合、留存清理策略和 analyst 仪表盘。第一方前端事件不作为资金、广告结算、订单或履约权威依据。本切片未部署、未修改共享 ECS、未调用真实 provider；注册、支付、履约、真实外部写操作和生产上线继续关闭。
- Git 状态：工作分支 `codex/d1-08-first-party-events`；D1-08 实现提交 `351e778` 已推送；GitHub ready PR #10 已创建并指向 `main`，待 Claude 审核，未批准或合并；用户未跟踪的 `docs/planning/` 保持未修改且不进入提交。

## D2：六类域名工具

### 2026-08-05（D2-01）

- 阶段状态：进行中；D2-01 域名标准化、IDNA 校验和混合书写系统风险识别完成，查询、WHOIS、DNS、SSL、价格及工具页接线待后续切片。
- 完成内容：新增浏览器/服务端可复用的 `normalizeDomain` 纯函数和 D1-02 `Result`/Problem Details 适配器；以精确版本 `tr46@6.0.0` 执行 Unicode 17 UTS-46 非过渡映射、Punycode 双向回转、STD3、Bidi 和 Joiner 校验，以官方 Unicode 17 IDNA2008 数据离线校验 PVALID/CONTEXTO/CONTEXTJ 并拒绝 emoji、未分配和不允许字符。统一处理大小写、首尾空白、全角字符、等价点和一个根尾点，校验空标签、63/253 ASCII 字节、字符集、连字符、纯数字 TLD 及无效/双重 `xn--`；输出 ASCII、显式 Unicode 转换值、固定等于 ASCII 的公开展示值和稳定错误码。`unicode-script@1.2.0` 按 UTS-39 resolved script 对每个标签识别混合书写系统，日文/韩文标准复合脚本不误报，风险提示不声称可注册或商标安全。未新增公开 endpoint、Payload schema、migration、provider 调用或工具页行为。
- 验证证据：目标域名测试 11 个通过，覆盖中文域名、直接 Punycode、全角/大小写/空白/尾点、`faß.de`、emoji、UTS-39 混合脚本、合法日文复合脚本、IDNA2008 上下文字符、63/253 边界、连字符、空标签、无效/双重 `xn--`、空输入、ASCII/非 ASCII 纯数字 TLD 和 Problem Details。最终 `make check` 通过生成类型/import map/schema 漂移、空库和遗留 migration 路径、Nginx、lint、TypeScript strict、178 个单元测试、26 个 PostgreSQL/MinIO 集成测试、Next.js 生产构建、linux/amd64 同镜像、依赖 high 门禁和 Gitleaks；依赖审计仍为既有 2 个 low 和 2 个 moderate，无 high/critical。`make test-e2e` 冷启动首次因 Next 开发服务器仍显示 `Compiling`、`/api/admins/me` 瞬时空响应而有 2 个既有管理员场景超时，其余 14 个通过；未修改代码直接完整复跑后 16 个场景全部通过。
- 遗留风险/门槛：当前混合书写系统检测不实现单脚本 confusable skeleton、商标库或法律判断；Unicode 值只供显式 IDN 转换，公共展示和后续 provider 输入必须使用 ASCII/Punycode。完整查询输入仍不落库；查询/价格 provider、WHOIS、DNS、SSL/CAA、工具页、真实外部写操作和生产上线继续关闭。首次 E2E 冷编译抖动作为既有开发服务器测试稳定性风险保留记录，本切片未扩大范围修正认证或测试基础设施。
- Git 状态：工作分支 `codex/d2-01-domain-normalization`；D2-01 实现提交 `3b8dc11` 与验证文档提交 `f7540c8` 已推送；GitHub ready PR #11 已创建并指向 `main`，待 Claude 审核，未批准或合并；用户未跟踪的 `docs/planning/` 保持未修改且不进入提交。

### 2026-08-05（D2-02）

- 阶段状态：进行中；D2-02 西部数码只读查询/价格适配器、客户端主动限频、相同请求合并和有界缓存完成，D2-03 多 TLD 可注册查询业务编排与六状态映射待后续切片。
- 完成内容：在 `src/providers` 新增可注入配置、时钟、请求 ID、结构化 logger 和 mock/fixture transport 的 `WestDigitalReadProvider`；可注册与普通价格入口均复用 D2-01 `normalizeDomain`，规范化后再按 ASCII 域名形成缓存/in-flight key，并按西部数码本地文档的 `v2/domain/query/` 与 `v2/info/` 请求/响应字段建立严格 Zod 解析。普通与溢价结果只暴露领域模型，整数人民币安全换算为整数分；负数、小数、溢出和字段不一致均作为畸形响应。进程内 token bucket 默认每秒 2 次、突发 4，FIFO 最多排队 32 个不同请求、最长等待 5 秒，transport 超时 5 秒且不自动重试；可注册性 LRU 为 45 秒/5,000 项，普通价格 LRU 为 1 小时/512 项，失败不缓存、不返回 stale。429、队列满、排队超时、上游超时、连接失败、畸形响应及 HTTP/业务拒绝均映射为稳定错误码、中文信息、`retryable`/可选 `retryAfterSeconds` 和可审计 `requestId`；日志只记录安全元数据，不记录完整域名、token、请求表单、响应正文或上游错误详情。`ALLOW_REAL_PROVIDER_WRITES=false` 保持不变，未新增真实鉴权、凭据、网络 transport、公开 endpoint、Payload schema、Hook 或 migration，现有 mock 履约写能力保持兼容。
- 验证证据：本地 fixture/单元测试覆盖查询与价格请求字段、普通/溢价响应、整数分换算、中文/Punycode/大小写/等价点归一化、非法输入 transport 零调用、2 次/秒与突发容量、FIFO、队列满/等待超时、in-flight 合并、两个 LRU 的命中/TTL/容量淘汰、失败不缓存且不回退 stale、429/超时/连接失败/畸形数据/业务拒绝的错误契约、脱敏日志以及 fixture 不调用全局 `fetch`。最终 `make check` 通过生成类型/import map/schema 漂移、空库及遗留 migration、Nginx、lint、TypeScript strict、190 个单元测试、26 个 PostgreSQL/MinIO 集成测试、Next.js 生产构建、linux/amd64 同镜像、依赖 high 门禁和 Gitleaks；依赖审计仍为既有 2 个 low 和 2 个 moderate，无 high/critical。`make test-e2e` 的 16 个 Playwright 场景一次通过。
- 遗留风险/门槛：限流、in-flight map 与 LRU 均为进程内状态，只适用于 P1 当前单 Web 进程；扩展为多 Web 实例前必须引入共享额度协调，但本切片不提前增加 Redis 或第二套后端。D2-03 才实现多 TLD 可注册查询、六状态映射、部分成功与购买报价编排；Who-Dat、DNS、TLS/CAA 综合降级项仍未完成。未调用真实西部数码接口或使用真实凭据，真实联调、外部写操作、部署和生产上线继续关闭。
- Git 状态：工作分支 `codex/d2-02-westdigital-read-adapter`；D2-02 实现与验证提交 `8145af8` 已推送；GitHub ready PR #12 已创建并指向 `main`，待 Claude 审核，未批准或合并；用户未跟踪的 `docs/planning/` 与本地西部数码 API 文档保持未修改且不进入提交。

### 2026-08-05（D2-02 西部数码文档约束补充）

- 阶段状态：D2-02 交付规范补充完成；不改变实现范围与验证结论。
- 完成内容：在仓库根目录 `AGENTS.md` 第 11 节新增强制前置规则，后续凡涉及西部数码 API 的调研、fixture/mock、字段或价格单位映射、transport、错误处理、联调和审核，均必须先查阅同目录本地文件 `西部数码业务API接口文档（v2）新.md`；禁止凭记忆、网络搜索或其他项目实现猜测接口契约。文件缺失、含糊或与代码冲突时必须停止相关实现并向项目负责人确认。该文档继续只读、不纳入版本控制，规则不扩大真实接口、凭据或写操作授权。
- 验证证据：仅修改执行规范与开发日志；`git diff --check` 通过，本地 API 文档仍存在且未被暂存，D2-02 已通过的 `make check` 与 `make test-e2e` 结论不受影响。
- 遗留风险/门槛：本规则保证当前仓库内代理使用统一的本地接口依据，但不会自动验证文档是否为西部数码最新版本；版本更新、真实联调和任何写操作仍需项目负责人另行确认或授权。
- Git 状态：D2-02 ready PR #12 已于 2026-08-05 由项目负责人合并，Codex 未执行合并；规范补充位于独立工作分支 `codex/westdigital-api-doc-rule`，实现提交 `a69f8db` 与交付状态记录已推送至指向 `main` 的 GitHub ready PR #13，待 Claude 审核，未批准或合并。用户未跟踪的 `docs/planning/` 与本地西部数码 API 文档保持未修改且不进入提交。

### 2026-08-05（D2-03）

- 阶段状态：进行中；D2-03 多 TLD 可注册查询、部分成功、六种业务状态和公开结果页完成，D2-04 Who-Dat/RDAP/WHOIS 待后续独立切片。
- 完成内容：新增共享严格 Zod 请求/响应 schema、`POST /api/v1/tools/domain-search` 和 `src/services/domain-search` 编排层；完整域名只查询自身后缀，关键词默认查询 `com`、`cn`、`net`、`org`、`top`、`xyz`、`vip`、`cc`、`tv`、`com.cn`，单次超过 10 个 TLD 返回稳定 `DOMAIN_SEARCH_TLD_LIMIT_EXCEEDED`，规范化重复和完整域名附带额外 TLD 均明确拒绝。最多 10 项通过 `Promise.allSettled` 独立调用 D2-02 `WestDigitalReadProvider`，逐项映射 `available`、`premium`、`registered`、`restricted`、`unsupported`、`query_failed`，聚合映射 D1-02 `ready`、`empty`、`partial`、`degraded`、`error`、`rate_limited`；每项展示西部数码或 Wanmi fixture 来源、查询时间、缓存命中状态和安全错误。扩展本地 transport 提供 premium/taken/reserved/partial/failed/ratelimited 确定性 fixture；`avail=0` 没有明确 fixture 证据时只返回 `WESTDIGITAL_STATUS_AMBIGUOUS`，不查询 WHOIS，也不把上游“不允许”推断为已注册。结果页客户端请求固定 `no-store`、`credentials: omit`、origin-only referrer，参数页保持 canonical 与 `noindex, nofollow`，`tool_completed`/`tool_failed` 只发送 TLD/状态/耗时桶等既有聚合字段。
- 验证证据：服务、endpoint、组件和 E2E 测试覆盖默认 10 TLD、完整域名/关键词、Unicode/Punycode、多标签后缀、六种业务状态、六种 Result 状态、单项抛错隔离、超限不截断、重复拒绝、缓存 miss/hit、429 重试提示、来源/时间/缓存展示、无购买入口以及 API/分析请求隐私。`make check` 通过生成类型/import map/schema 漂移、空库及 D1 遗留 migration 往返、Nginx、lint、TypeScript strict、200 个单元测试、26 个 PostgreSQL/MinIO 集成测试、Next.js 生产构建、linux/amd64 同镜像、依赖 high 门禁和 Gitleaks；依赖审计保持既有 2 low、2 moderate，无 high/critical。`make test-e2e` 的 17 个 Playwright 场景通过；镜像内静态生成无法连接未随构建容器启动的 `127.0.0.1:5432` 时按既有公共内容安全回退完成构建。
- 遗留风险/门槛：当前全部查询为显式标记的本地 fixture，默认 TLD 与已注册/保留证据是服务端固定目录，不是实时西部数码结论；真实读取 transport、能力/价格验证、运营后台 TLD 配置、普通与续费价格、购买入口和真实联调均未开放。D2-02 限流、in-flight 和 LRU 仍为进程内状态，仅适配 P1 当前单 Web 进程；未新增查询数据库、Local Storage 或完整域名分析字段。Who-Dat、DNS、TLS/CAA、真实写操作、部署和生产上线继续关闭。
- Git 状态：工作分支 `codex/d2-03-domain-search` 基于已合并 PR #13 后的 `origin/main`；D2-03 实现与验证提交 `38f6407` 已推送，GitHub ready PR #14 已创建并指向 `main`，待 Claude 审核，Codex 未批准或合并。用户未跟踪的 `docs/planning/` 与本地西部数码 API 文档保持未修改且不进入提交。

### 2026-08-05（D2-04）

- 阶段状态：进行中；D2-04 Who-Dat/RDAP/WHOIS 查询、故障降级、公开结果页及与可售状态严格分离完成，DNS 与 TLS/SSL/CAA 待后续独立切片。
- 完成内容：新增独立 `PublicRegistrationProvider`、严格共享 Zod、`POST /api/v1/tools/whois`、WHOIS 服务编排和全部可见状态结果组件；Who-Dat v2.0.0 按 RDAP 优先/内部 WHOIS 覆盖返回公开记录，严格校验并有界读取响应，使用 5.5 秒超时、主动限频、有界排队和同域名 in-flight 合并。Who-Dat 429 不绕过限流；501/502/504、连接/超时、重定向、响应过大或畸形时，仅在显式开关和完整凭据下调用独立西部数码 `act=whois` HTTPS transport，使用本地文档的 13 位毫秒鉴权和 GB18030 有界解码，成功映射 `degraded`。联系人、邮箱、电话、地址、原始 WHOIS/RDAP body、上游 URL/request ID 及西部数码 `clientid` 不公开、不记录；`no_public_record/empty` 明确不代表可注册，代码和页面均不导入 D2-03 可售 provider/schema、不显示购买入口。API 固定严格 4 KiB JSON、`no-store` 和请求 ID，复用 D2-01 规范化并在 provider 前拒绝 IP literal、loopback/私网/link-local/ULA、元数据/本地域名、URL、端口、路径和单标签；查询域名不会被 DNS 解析或建立 HTTP/TLS 连接。结果页展示数据源、查询时间、缓存状态与字段缺失说明，参数页保持 `noindex, nofollow` 和干净 canonical/OG，浏览器请求不带 Cookie、只发送 origin referrer，分析事件不含完整域名。Who-Dat 内存缓存固定 1 小时、无持久卷，容器访问日志有界轮转；未新增 Payload schema、migration、数据库或 Web Storage。
- 验证证据：单元测试覆盖 Who-Dat RDAP/WHOIS/cache/no-record、400/401/429/501/502/504、连接/超时/重定向/过大/Content-Type/JSON/schema 异常、限频队列和 in-flight 合并；覆盖西部数码 HTTPS origin、`act=whois`、毫秒时间戳/MD5 鉴权、GB18030、字段脱敏、空数据不推断、429/超时/过大；覆盖 Unicode/Punycode、完整域名、SSRF 目标、429 不降级、故障降级、双失败、六状态契约、严格 4 KiB/no-store/request ID、UI 缺失字段与可售分离。`make check` 通过生成类型/import map/schema 漂移、空库和遗留 migration 往返、Nginx、lint、TypeScript strict、247 个单元测试、26 个 PostgreSQL/MinIO 集成测试、Next.js 生产构建、linux/amd64 同镜像、依赖 high 门禁和 Gitleaks；依赖审计仍为既有 2 low、2 moderate。`make test-e2e` 的 17 个 Playwright 场景通过，覆盖 390px WHOIS 页面、来源/时间/缓存、noindex/canonical/OG、无购买入口、Cookie/referrer 隔离和分析事件最小化。
- 遗留风险/门槛：西部数码 WHOIS fallback 默认关闭，真实部署必须通过受控环境同时提供开关、用户名和 API 密码；本切片全部西部数码测试使用 fixture，未发送真实请求或使用真实凭据。限流、队列和 in-flight 仍为当前 P1 单 Web 进程内状态；Who-Dat 一小时缓存由其容器内存承担，不在 Web 层保存 stale。DNS、TLS/SSL/CAA、浏览器历史/收藏、真实 provider 写操作、部署和生产上线继续关闭。
- Git 状态：工作分支 `codex/d2-04-whois-rdap` 基于已合并 PR #14 后的 `origin/main`；D2-04 实现与验证提交 `2d2d8e9` 已推送，GitHub ready PR #15 已创建并指向 `main`，待 Claude 审核，Codex 未批准或合并。用户未跟踪的 `docs/planning/` 与本地西部数码 API 文档保持未修改且不进入提交。

### 2026-08-05（D2-05）

- 阶段状态：进行中；D2-05 八类 DNS/NS 只读查询、严格 SSRF 防护、资源控制、公开 API 与结果页完成，D2-06 TLS/SSL/CAA 检查待后续独立切片。
- 完成内容：新增共享严格 Zod、`DnsReadProvider`、`POST /api/v1/tools/dns` 和 Node runtime 服务编排，一次并发查询 A、AAAA、CNAME、MX、TXT、NS、SOA、CAA，保留全部记录 TTL、SOA 时序字段和原始 CAA flags/tag/value；复用 D2-01 域名规范化并返回 Unicode/Punycode 和混合书写系统风险。DoH 端点只允许代码内硬编码的阿里公共 DNS `223.5.5.5` 与 `223.6.6.6`，不接受请求或环境变量指定解析器、不使用系统 DNS、不跟随重定向；二进制响应严格校验事务 ID、单 Question、名称/类型/CLASS、响应/递归/截断标志、RCODE 和回答链。直接 A/AAAA 以及 CNAME、MX、NS、SOA 间接目标都通过同一受控解析器验证，只允许公网单播；loopback、私网、CGNAT、link-local、保留/文档、组播、IPv4-mapped 和云元数据目标均失败关闭，隐藏相关记录集和值。默认单次 3 秒、64 KiB、每类型 32 条/总计 128 条、最多 16 个唯一目标和 8 个并发；全局 20 次/秒、突发 40、有界 64 队列和 2 秒等待。相同域名/类型 in-flight 合并，成功/负结果按自身与目标验证 TTL 最小值缓存且上限 60/30 秒、LRU 4,096 项，失败、安全阻断和 stale 不缓存。结果区分 `records/no_record/nxdomain/servfail/timeout/blocked/failed/rate_limited` 并聚合为 D1-02 六状态；NXDOMAIN 明确不代表可注册。页面展示解析来源/节点、时间、缓存和 Unicode/Punycode，参数页 `noindex, nofollow` 且 canonical/OG 干净，请求固定 `no-store`、`credentials: omit`、origin-only referrer，分析不含完整域名；无购买、管理、DNS 写入或托管入口。
- 验证证据：单元、路由和组件测试覆盖八类报文/TTL、Unicode、严格 schema/4 KiB、RCODE、主备降级、缓存/in-flight、超时、64 KiB/32 条/128 条/16 目标/8 并发、队列受限和脱敏日志；SSRF 矩阵覆盖 IPv4/IPv6 直接地址及 CNAME/MX/NS/SOA 间接指向 loopback、私网、link-local、CGNAT、元数据、保留/文档与 IPv4-mapped 地址，失败结果不披露受限地址。`make check` 通过生成类型/import map/schema 漂移、空库与遗留 migration、Nginx、lint、TypeScript strict、325 个单元测试、26 个 PostgreSQL/MinIO 集成测试、依赖 high 门禁、Gitleaks、Next.js 生产构建和 linux/amd64 同镜像。依赖审计仍为既有 2 low、2 moderate，无 high/critical。`make test-e2e` 最终 18 项通过，覆盖 390px 八类记录、来源/时间/缓存、Unicode/Punycode、NXDOMAIN 非可注册、noindex/canonical/OG、Cookie/referrer 和分析隐私；首次两次完整运行分别有 1/2 个既有 Admin 登录在 dev 首编译时命中默认 5 秒断言，调整相关 URL 断言为 15 秒后并行定向 2 项和完整 18 项均通过。全部 DNS 自动化测试使用注入 fixture，未访问真实 DNS。
- 遗留风险/门槛：限流、队列、in-flight 和 LRU 为 P1 当前单 Web 进程内状态，多 Web 实例前需共享额度协调；结果只代表阿里公共 DNS 单一递归视角，不保证全球即时一致。固定外部解析器的生产连通性和告警仍需部署阶段验证。本切片不建立 SSL/TLS 连接、不执行 CAA 父域遍历或签发策略判断，不新增 Payload schema/migration、长期查询存储、DNS 写入或托管；未部署、未修改共享 ECS，真实 provider 写操作和生产上线继续关闭。
- Git 状态：工作分支 `codex/d2-05-dns-readonly` 基于已合并 D2-04 的最新 `origin/main`；D2-05 实现与验证提交 `1221eef` 已推送，GitHub ready PR #16 已创建并指向 `main`，待 Claude 审核，Codex 未批准、合并或部署。用户未跟踪的 `docs/planning/` 保持未修改且不进入提交。

### 2026-08-05（D2-06）

- 阶段状态：进行中；D2-06 固定 443 的 TLS/SSL 证书与 CAA 安全检查、公开 API、六状态结果页和高风险出站边界完成，D2 后续价格、IDN 工具页、浏览器历史/收藏、工具跳转分享及监控待独立切片。
- 完成内容：新增共享严格 Zod、`TlsHandshakeProvider`、`POST /api/v1/tools/ssl-check`、TLS/CAA 编排和结果组件；请求只接受完整域名并拒绝 IP、URL、端口、本地/元数据目标和额外控制字段。复用 D2-05 AliDNS、TTL 缓存、主备错误映射及 `isPublicDnsAddress` 查询/验证 A、AAAA、CAA；任一受限地址即整体阻断并隐藏地址。TLS provider 最多接收 8 个已验证 IP、IPv6/IPv4 稳定交错、最多尝试 4 个，以 IP 和固定 443 建立 TCP，禁用地址族自动选择并在连接后复核远端 IP/端口；域名只用于 SNI 与原生 `checkServerIdentity`，从结构上避免重新解析和 DNS rebinding/TOCTOU。Duplex 统计入站握手并硬限 256 KiB，总时限 5 秒，成功读取证书后禁用 renegotiation、立即销毁且不收发应用数据。证书展示有效期/剩余天数、域名匹配、主题、签发者、SAN、信任/自签名/无效链和最多 10 层链摘要；CAA 按 RFC 8659 逐级继承最多 16 层，未知层停止，解释 `issue`/`issuewild`/`iodef`/critical/空 `issue`，不访问 iodef、不推断当前证书 CAA 合规。进程内 TLS 控制为 10 次/秒、突发 20、并发 4、队列 32/等待 2 秒；完整结果 60 秒、无地址 30 秒、LRU 2,048 项，同域 in-flight 合并且 trace ID 隔离，安全/运行失败无缓存/stale。页面展示组件与聚合来源、时间、缓存、请求 ID 和 Unicode/Punycode；参数页 noindex/canonical/OG、API no-store，客户端省略 Cookie、只发送 origin referrer，分析只含 TLD、状态、耗时桶和 `tls` 来源。没有 HTTP 抓取、重定向、OCSP、证书签发、私钥处理、Payload schema/migration 或长期查询存储。
- 验证证据：定向单元测试覆盖严格 4 KiB/未知字段、六状态、CAA 当前域/父域继承/无记录/critical/空 issue/issuewild/iodef/16 层/未知层停止/主备与缓存；SSRF 矩阵覆盖 IPv4/IPv6 私网、回环、link-local、CGNAT、元数据、保留/文档、组播和 IPv4-mapped，受限地址下 connector 零调用且结果不泄漏地址。运行时生成有效、自签名和 CA 签发但缺链证书，在本机临时 TLS/raw server 验证有效、过期、尚未生效、不匹配、自签名、链无效、拒绝连接、总超时、握手超限、IPv6/IPv4 轮换、最多 4 次、并发/队列、远端地址复核、固定 443 和零应用数据；同域 in-flight 复用不泄漏首请求 trace。最终 `make check` 通过生成类型/import map/schema 漂移、空库及遗留 migration 往返、Nginx、lint、TypeScript strict、365 个单元测试、26 个 PostgreSQL/MinIO 集成测试、Next.js 生产构建、linux/amd64 同镜像、依赖 high 门禁和 Gitleaks；依赖审计仍为既有 2 low、2 moderate，无 high/critical。`make test-e2e` 完整运行 19 个 Playwright 场景通过，覆盖 390px TLS/证书链/CAA、来源/时间/缓存、noindex/canonical/OG、Cookie/referrer 和分析事件隐私。在后续重复并行复跑期间，本机 PostgreSQL 单个后端进程异常退出并触发 crash recovery，造成多个既有页面级联失败；容器未重启且未 OOM，数据库恢复 healthy 后清理本仓库遗留的 Next/Playwright 进程，不放宽既有断言超时，以同一 Playwright 配置显式单 worker 完整复跑 19/19 通过。全部自动化测试未访问真实公网。
- 遗留风险/门槛：限流、队列、in-flight 与 LRU 仍为 P1 单 Web 进程内状态，多 Web 实例前必须引入共享额度协调；结果只代表查询时的阿里公共 DNS 视角和 Node 运行时信任库，不执行 OCSP/CRL 在线吊销查询，也不证明未来签发满足 CAA。生产出站防火墙、固定 DNS/TLS 端点连通性、告警及 Node CA 库更新策略仍需部署阶段验证。综合“西部数码 429、队列满、Who-Dat 失败、DNS/TLS 超时”任务仍保持未完成；未部署、未修改共享 ECS、未调用真实公网 TLS 或任何外部写接口，生产上线继续关闭。
- Git 状态：工作分支 `codex/d2-06-tls-ssl-caa` 基于已合并 PR #16 的最新 `origin/main`；D2-06 实现与验证提交 `e2d5bb5` 已推送，GitHub ready PR #17 已创建并指向 `main`，交 Claude 审核。Codex 未批准、合并或部署；用户未跟踪的 `docs/planning/` 保持未修改且不进入提交。

### 2026-08-06（D2-07）

- 阶段状态：进行中；D2-07 TLD 注册/续费价、1 年/3 年成本、可复现价格快照、公开 API 和价格页完成，D2 后续 IDN 工具页、浏览器历史/收藏、工具跳转分享及监控待独立切片。
- 完成内容：先将本地 `main` 快进到已合并 PR #17 的 `origin/main`，再建立独立分支 `codex/d2-07-tld-pricing`。复用 D2-02 `WestDigitalReadProvider.queryPrice({ years: 1 })` 和 fixture transport，为默认 10 个 TLD 建立明确非实时的合成整元成本；9 个已配置后缀每项只查询一次，3 年成本不重复查询 provider，`tv` 因未配置加价规则在 provider 前跳过并明确不开放购买。固定加价和 basis points 比例加价均分别作用于注册/每年续费成本，金额和规则只接受非负安全整数，比例使用 BigInt 中间值及半入到分，1 年/3 年按固定公式计算，异常/溢出/快照失败均失败关闭。新增整数安全人民币分格式化，并替换既有溢价 fixture 的浮点展示。新增仅 `system_admin` 可读、所有通用 create/update/delete 均关闭且不参与文档锁定的 `priceSnapshots`；系统快照服务显式 `overrideAccess: true`，保存完整 provider 成本、规则/版本、舍入、公式、终价、时间、缓存、请求与 trace 证据，稳定 calculation hash 覆盖计算输入和输出，缓存命中复用引用，并发唯一冲突幂等收敛。provider 失败只按 TLD、规则、schema/计算版本回退最近快照并公开 stale 终价；公共响应不包含上游成本、加价金额或规则。新增严格共享 Zod 和 4 KiB JSON 的 `POST /api/v1/tools/pricing`、逐项五状态与聚合六状态；`/pricing` 展示默认 10 项的注册/续费、最低年限、1/3 年成本、来源、取价时间、缓存和快照引用，明确普通/溢价边界、fixture 边界和交易未开放。浏览器请求不带 Cookie、`no-store`、origin-only referrer，分析只含聚合维度。生成并提交 Payload migration JSON/index 与 `payload-types.ts`，迁移验证脚本新增快照列、唯一/检索索引、无多余锁表关系及 down/up 往返门禁。
- 验证证据：计算、服务、路由、组件测试覆盖固定/比例加价、半入、安全整数/溢出、稳定 hash、完整重放、miss→hit 引用复用、并发幂等、规则未配置 provider 零调用、每配置 TLD 单次 `years: 1`、写入失败关闭、历史 stale、部分失败、429 和全部六状态、严格 body/schema/Problem Details、无内部成本/规则泄露及无购买入口；PostgreSQL 集成覆盖唯一 calculation hash、最近快照及匿名/customer/四类管理员权限矩阵。首次 `make check` 的 377/378 单元项通过并发现后台分组预期漏列 `priceSnapshots`，补齐后完整复跑通过：生成/schema 漂移、空库/遗留升级/D2-07 down/up、Nginx、lint、TypeScript strict、378 个单元测试、28 个 PostgreSQL/MinIO 集成测试、Next.js 生产构建、linux/amd64 同镜像、依赖 high 门禁和 Gitleaks 均通过；依赖审计为既有 2 low、2 moderate，无 high/critical。价格 Playwright 定向场景通过；两次四 worker 全量分别因既有 Admin 固定 5 秒首编译断言和一次第一方 endpoint `ECONNRESET` 得到 18/20、19/20，PostgreSQL 持续 healthy、无重启/OOM；最终 `CI=1 make test-e2e` 全量 20/20 通过，覆盖默认 10 项、`tv` 未配置、miss→hit 同一 `snapshotRef`、来源/时间/缓存、无 Cookie、无成本/规则泄露、分析最小化和无购买入口。
- 遗留风险/门槛：全部价格仅为明确标识的本地 fixture，不代表实时西部数码价格或可购买承诺；真实只读 transport、运营价格规则、溢价域名、5 分钟客户报价/锁价、登录、下单、支付和订单状态机均不在本切片，D5 报价必须重新验证并建立独立 `quotes`。D2-02 限流/LRU 仍为 P1 单 Web 进程内状态；内部计算快照当前不含个人信息且不自动删除，后续保留策略须单独评审。未部署、未修改共享 ECS、未调用真实 provider 或任何外部写接口。
- Git 状态：工作分支 `codex/d2-07-tld-pricing` 基于已合并 PR #17 后的最新 `origin/main`；D2-07 实现与验证提交 `7fa59d5` 已推送，GitHub ready PR #18 已创建并指向 `main`，交 Claude 审核。Codex 未批准、合并或部署；用户未跟踪的 `docs/planning/` 与本地西部数码 API 文档保持未修改且不进入提交。

### 2026-08-06（D2-08）

- 阶段状态：进行中；D2-08 IDN/Punycode 本地双向转换、精确非法标签错误、混合书写系统风险、公共六状态 API 契约和结果页 SEO 边界完成，D2 后续浏览器历史/收藏、通用分享入口、监控及综合降级测试待独立切片。
- 完成内容：基于已合并 D2-07 的最新 `main` 建立 `codex/d2-08-idn-conversion`。直接增强 D2-01 `normalizeDomain`，没有复制 TR46、IDNA2008 或 UTS-39 实现；`DomainValidationError` 新增可选的一基 `labelPosition`，标签错误明确指出“第 N 个标签”和具体原因，域名总长、空输入等保持域级错误。空标签、非法字符、连字符、坏 Punycode、上下文字符、63 字节标签、253 字节域名和纯数字 TLD 均使用稳定 code；混合脚本提示列出风险标签及拉丁文（Latin）、西里尔文（Cyrillic）等实际脚本，并固定包含“转换成功不代表可注册或商标安全”。规范化输出保持 `display === ascii`。新增严格共享 Zod 与 `POST /api/v1/tools/idn`：请求体最多 4 KiB、`query` 最多 1,024 字符，语义错误以 HTTP 200 六状态 `Result` 的 `error` 返回，Content-Type、畸形 JSON、缺失/未知字段和超大请求使用标准 Problem Details；响应固定 `no-store` 和安全请求 ID，路由仅调用纯函数。将 `/tools/idn` 替换为单输入可访问 Client Component，自动识别 Unicode、Punycode 和普通 ASCII，浏览器本地同步转换且不调用 IDN API、不改变普通提交 URL；Punycode 作为主结果、公开展示、默认复制及域名查询/WHOIS/DNS/SSL 链接参数，Unicode 仅为显式预览，两种结果都有复制反馈。干净页保持可收录，直接 `?q=` 本地初始化并保持 `noindex, nofollow`、干净 canonical/OG；事件只发送 `idn`、`local`、结果类别、耗时桶等聚合字段，不发送 TLD、输入、Unicode 或 Punycode。
- 验证证据：22 项 IDN 定向单元/组件/路由测试覆盖中英文回转、直接 A-label、全角/大小写/根点、非法字符、空标签、连字符、坏 Punycode、上下文字符、标签/总长边界、数字 TLD、一基标签位置、脚本名称/免责声明、ready/error 与六状态可解析、严格 JSON/实际流式 4 KiB/1,024 字符/未知字段、`no-store`/请求 ID、本地转换、复制反馈、ASCII 跨工具链接和分析/API 隐私；390px Playwright IDN 场景定向通过。最终 `make check` 通过生成类型/import map/schema 漂移、空库及遗留 migration、Nginx、lint、TypeScript strict、389 个单元测试、28 个 PostgreSQL/MinIO 集成测试、Next.js 生产构建、linux/amd64 同镜像、依赖 high 门禁和 Gitleaks；依赖审计仍为既有 2 low、2 moderate，无 high/critical。首次完整 E2E 因既有第一方事件 endpoint 一次 `ECONNRESET` 为 20/21，单项复跑通过；第二次因既有价格页冷编译超过固定 5 秒等待为 20/21，失败快照已含目标标题和完整表格；未放宽断言，随后隐私收口前后两次同一 `make test-e2e` 均全量 21/21 通过，IDN 场景所有全量运行均通过。
- 遗留风险/门槛：UTS-39 当前提示标签内混合书写系统，不实现单脚本 confusable skeleton、商标数据库、注册局政策或法律判断；转换成功不代表域名可注册、可访问或商标安全。API 只为公共契约保留，站内页面刻意不调用；本切片没有查询注册状态、WHOIS、DNS、价格或购买，没有 provider、外网、数据库、Payload schema、migration、生成类型或长期查询存储。未部署、未修改共享 ECS、未执行任何外部写操作，生产上线继续关闭。
- Git 状态：工作分支 `codex/d2-08-idn-conversion` 基于已合并 PR #18 的最新 `origin/main`；D2-08 实现与验证提交 `1a5fdae` 已推送，GitHub ready PR #19 已创建并指向 `main`，交 Claude 审核。Codex 未批准、合并或部署；用户未跟踪的 `docs/planning/` 保持未修改且不进入提交。

### 2026-08-06（D2-09）

- 阶段状态：进行中；D2-09 浏览器本地历史与收藏、隐私控制、集中管理及跨入口同步完成，D2 后续通用复制/分享、监控及综合降级测试待独立切片。
- 完成内容：基于已合并 D2-08 的最新 `origin/main` 建立 `codex/d2-09-local-history-favorites`。新增独立 history/favorites v1 `localStorage` 键、严格 Zod 运行时校验和安全降级；每次读写清理损坏、90 天过期和超过 30 条的最旧项，历史按工具与规范化查询去重，完整域名收藏按 D2-01 ASCII/Punycode 去重并保留 Unicode 展示，工具收藏与域名收藏使用固定路由。只有用户主动提交有效非空查询才写历史，直接 `?q=`、刷新和价格页不写；DNT/GPC 阻止新增或更新时间，但已有记录仍可查看/删除，用户主动收藏与清空仍可用。`/tools` 增加历史/收藏管理、可访问反馈、再次查询、单项删除、分项清空和真正移除两个键的全部清空；工具卡、当前完整域名、域名结果卡和有效 IDN 结果增加收藏入口。同页自定义事件和跨标签 `storage` 事件通过前端布局内唯一 Provider 同步，小型 Client Components 隔离浏览器 API，页面继续保持 Server Components。收藏、删除、清空和历史打开不发第一方事件，重新查询只沿用既有不含完整查询值的聚合结果事件。
- 验证证据：本地库与管理面板定向单元测试 7/7、相关五文件定向测试 19/19、新增 Playwright 隐私/本地存储场景 2/2。`make check` 通过生成类型/schema 漂移、空库与遗留 migration、Nginx、lint、TypeScript strict、396 个单元测试、28 个 PostgreSQL/MinIO 集成测试、Next.js 生产构建、linux/amd64 同镜像、依赖 high 门禁和 Gitleaks；依赖审计维持既有 2 low、2 moderate，无 high/critical。完整 `make test-e2e` 两次均为 22/23，唯一失败是既有 Admin 一次性邀请场景在并发 Next 冷启动中首次登录/绑定超过固定 5 秒，随后重试无法复用已消费邀请；失败与 D2-09 页面、存储和事件链路无交集，未放宽断言，同一 Admin 文件以 CI 配置单 worker 隔离复核 3/3 通过。
- 遗留风险/门槛：数据严格只存在当前浏览器 profile，没有账号同步、跨设备恢复或服务端备份；浏览器禁用/损坏/配额或隐私模式限制 Storage 时仅安全降级，用户清理站点数据后不可恢复。DNT/GPC 只关闭自动历史，显式收藏是用户主动本地操作。未新增 API、Payload schema、migration、生成类型、环境变量或服务端同步，未上传查询记录、未部署、未修改共享 ECS、未执行任何外部写操作。
- Git 状态：工作分支 `codex/d2-09-local-history-favorites` 基于已合并 D2-08 的最新 `origin/main`；D2-09 实现与验证提交 `ad4b376` 已推送，GitHub ready PR #20 已创建并指向 `main`，交 Claude 审核。Codex 未批准、合并或部署；用户未跟踪的 `docs/planning/` 保持未修改且不进入提交。

### 2026-08-06（D2-10）

- 阶段状态：进行中；D2-10 六类工具跳转、确定性复制与隐私分享入口完成，D2 后续监控及综合降级测试待独立切片。
- 完成内容：基于已合并 D2-09 的最新 `origin/main` 建立 `codex/d2-10-tool-actions-sharing`。集中定义六工具元数据、安全路由和分享 URL 构造器；每个工具页展示其余五类入口，只有有效完整域名以规范化 Punycode `q` 传给查询工具，关键词不透传，价格页始终使用干净 `/pricing`，域名搜索的每个候选结果提供独立操作。新增复用复制按钮与可访问成功/失败反馈，覆盖域名、可售状态、WHOIS 字段/状态/NS、DNS zone 单条、单个 TLD 公开价格、IDN Punycode、TLS/证书/SAN/证书链/CAA；域名型字段统一复制 Punycode，IDN 删除 Unicode 复制。新增 Radix 分享确认弹层，每次打开默认只分享工具入口，用户必须主动改选并再次确认才生成带 Punycode 域名的链接；链接只允许当前 HTTP(S) origin、固定工具路径和可选 `q`，不读取当前 URL 的其他参数，不包含完整查询结果、trace/request/cache 标识或快照引用，不调用 Web Share、第三方 SDK、网络接口或分析事件。页面继续以 Server Component 为主，剪贴板和弹层收敛在小型 Client Components；带参数结果页继续 `noindex, nofollow` 和干净 canonical。
- 验证证据：新增路由/分享/复制格式化与交互单元测试，并扩充六类结果组件测试；定向四项 Playwright 核心场景 4/4。`make check` 通过生成类型/schema 漂移、空库与遗留 migration、Nginx、lint、TypeScript strict、403 个单元测试、28 个 PostgreSQL/MinIO 集成测试、Next.js 生产构建、linux/amd64 同镜像、依赖 high 门禁和 Gitleaks；依赖审计维持既有 2 low、2 moderate，无 high/critical。完整 `make test-e2e` 23/23 通过，覆盖 390px WHOIS 到同域名 DNS/SSL、域名候选操作、DNS 单条复制、IDN 仅复制 Punycode、默认及显式确认分享、内部参数隔离和结果页 noindex。
- 遗留风险/门槛：剪贴板能力受浏览器权限和安全上下文约束，失败时组件明确提示并保留可见原文供手动选择；含域名分享只在用户当次显式选择并确认后生成，默认链接没有域名，也没有结果快照或运行元数据。未新增或修改 `/api/v1` endpoint、Payload schema、migration、生成类型、环境变量或事件 schema，未调用 provider、未部署、未修改共享 ECS、未执行任何外部写操作。
- Git 状态：工作分支 `codex/d2-10-tool-actions-sharing` 基于已合并 D2-09 的最新 `origin/main`；D2-10 实现、文档与验证提交 `d5bb829` 已推送，GitHub ready PR #21 已创建并指向 `main`，交 Claude 审核。Codex 未批准、合并或部署；用户未跟踪的 `docs/planning/` 保持未修改且不进入提交。

### 2026-08-06（D2-11）

- 阶段状态：进行中；D2-11 六类工具、provider 错误与限频/队列聚合监控完成，D2 综合降级测试仍待独立切片。
- 完成内容：基于已合并 D2-10 的最新 `origin/main` 建立 `codex/d2-11-tool-observability`。复用 D1-08 的六类工具枚举、终态第一方事件和六档耗时桶，以每工具/每小时聚合请求、成功、失败、整数基点成功率及 P50/P95；复用 D1-07 的结构化脱敏 logger 包装现有 WestDigital、Who-Dat、AliDNS 和 Node TLS 适配器，只从固定 outcome 事件白名单提取 provider/操作、耗时桶、队列深度、拒绝标记，并将错误固定分类为 timeout、rate_limited、upstream_error、invalid_response。新增系统内部 Payload 聚合 Collection、命名 migration、snapshot 与生成类型；bucket key 唯一，进程内按桶串行收敛，并发首次创建以唯一约束重读更新。Collection 仅保存小时、固定 tool/provider/operation、计数、分桶分位数和队列聚合，不保存完整域名、查询值、TLD、IP、Cookie、URL、User-Agent、trace/request/session/client ID；所有通用写入口关闭，active analyst 与 system_admin 只读，原始第一方事件仍仅 system_admin 可见。provider 指标异步尽力持久化，失败只写脱敏运维日志且不阻断工具。
- 验证证据：13 项新增单元测试覆盖六工具、小时桶、P50/P95、并发聚合、四类 provider 错误、队列/拒绝计数、日志白名单与异步失败隔离；2 项新增 PostgreSQL 集成测试覆盖真实聚合持久化、隐私字段、分位数和完整权限矩阵，Playwright 覆盖 analyst 后台可见/只读、其他角色隐藏/拒绝及聚合 fixture 回收。最终 `make check` 通过生成类型/schema 漂移、空库/遗留升级/D2-11 down-up、隐私列与索引、Nginx、lint、TypeScript strict、416 个单元测试、30 个 PostgreSQL/MinIO 集成测试、Next.js 生产构建、linux/amd64 同镜像、安全门禁和 Gitleaks；依赖审计维持既有 2 low、2 moderate，无 high/critical。完整 `make test-e2e` 最终 23/23 通过；此前两轮分别有本地 dev 冷编译导致的既有 Admin 超时/瞬时内容 500，以及复用 dev server 造成价格首请求已命中缓存，未放宽断言，清理本地状态后以同一命令全量通过。
- 遗留风险/门槛：工具聚合沿用 D1-08 浏览器终态事件，尊重 DNT/GPC 的客户端不会进入工具级统计；provider 级日志指标仍记录服务端实际调用。当前桶内写入串行化和各 provider 限流/队列均符合 P1 单 Web 进程假设，多 Web 实例前需改为数据库原子累加或共享聚合协调；异步 provider 指标在进程强制退出时可能丢失尾部样本。告警通知、外部时序库、Prometheus/Grafana 和第三方 APM 均不在本切片；未部署、未修改共享 ECS、未调用真实 provider 或执行外部写操作。
- Git 状态：工作分支 `codex/d2-11-tool-observability` 基于已合并 D2-10 的最新 `origin/main`；实现提交 `2321a66` 与本条文档提交均纳入 GitHub ready PR #22 并指向 `main`，交 Claude 审核。Codex 未批准、合并或部署；用户未跟踪的 `docs/planning/` 与本地西部数码 API 文档保持未修改且不进入提交。

### 2026-08-06（D2-12）

- 阶段状态：完成；D2-12 综合降级测试及 D2 全部十二项任务完成，后续生产联调和上线门槛不因本切片放宽。
- 完成内容：基于已合并 PR #22 的最新 `main` 建立 `codex/d2-12-degradation-tests`。复用现有 fixture、可注入 provider mock 和 Playwright 路由拦截，新增西部数码 429/队列满/排队超时、Who-Dat 超时与不可用、DNS 单项/全量超时、TLS 超时与握手失败的跨层降级矩阵；覆盖 `degraded`、`partial`、`error`、`rate_limited`，并断言部分失败不拖垮独立结果、无数据错误不携带伪造结果、WHOIS/DNS/TLS 失败不出现购买入口或错误“可注册”结论。唯一生产代码修正是复用统一分类器，让域名可售和价格服务将 `WESTDIGITAL_QUEUE_FULL`、`WESTDIGITAL_QUEUE_TIMEOUT` 与显式限流一致映射为 `rate_limited`/HTTP 429；未改变 schema、Payload Collection、migration 或生成类型。
- 验证证据：定向 Vitest 5 个文件 120/120 通过，定向 D2-12 Playwright 4/4 通过；最终 `make check` 通过生成类型/import map/schema 漂移、空库/遗留升级/down-up migration、Nginx、lint、TypeScript strict、423 个单元测试、30 个 PostgreSQL/MinIO 集成测试、Next.js 生产构建、linux/amd64 同镜像、安全门禁和 Gitleaks；依赖审计维持既有 2 low、2 moderate，无 high/critical。完整 `make test-e2e` 27/27 通过。新增 Playwright 首轮仅因 Next route announcer 同样具有 `role=alert` 造成选择器歧义，收紧到实际可见 Alert 组件后同一场景及完整门禁均通过，未放宽业务断言。全部测试使用本地 fixture/mock/路由拦截，零真实西部数码、Who-Dat、DNS 或公网 TLS 调用。
- 遗留风险/门槛：本切片验证的是确定性的自动化降级契约，不替代后续受控环境中的真实 provider 只读联调、生产网络超时参数、告警阈值和 D7 运行演练；现有进程内限流、队列、in-flight 与缓存仍受 P1 单 Web 进程假设约束，多 Web 实例前需共享协调。未部署、未修改共享 ECS、未执行任何 provider 写操作或生产数据变更，真实收款与生产上线继续关闭。
- Git 状态：工作分支 `codex/d2-12-degradation-tests` 基于已合并 PR #22 的最新 `main`；实现提交 `993887c` 与本条文档提交均纳入 GitHub ready PR #23 并指向 `main`，交 Claude 审核。Codex 未批准、合并或部署；用户未跟踪的 `docs/planning/` 与本地西部数码 API 文档保持未修改且不进入提交。

## D3：内容、广告、分析与运营后台

### 2026-08-06（D3-01）

- 阶段状态：进行中；D3-01 轻量 CMS、完整内容工作流与受控富文本/预览完成，D3 广告、运营仪表盘、内容关联、完整 SEO/Redirect/Sitemap 和发布 Runbook 待后续独立切片。
- 完成内容：基于最新 `main` 建立 `codex/d3-01-content-cms`。在既有 `articles`、`topics`、`tldPages` 上新增 `helpPages`、`categories`、`tags`，四类内容统一保留 drafts、autosave 和 50 个版本；新增 `workflowStatus`、调度/发布时间、`revisionBy` 与发布必填 `source`。状态服务只允许 `draft → in_review → published → unpublished → archived`，支持 published 同状态发布修订，归档后只读；Zod 管理 API、Collection hook 与字段 access 阻止通用 REST、Local API 或 Admin 直接绕过，每次动作复用 D1-07 统一审计记录操作者、时间、类型、文档、前后状态及调度信息。关闭 Payload 原生定时入口，新增 `publishing` 队列自定义 Job，以文档为并发键并处理替换、取消、旧任务、重复执行和调度人权限失效。Lexical 仅启用段落、H2～H4、强调/下划线/行内代码、列表、引用、受控链接与 Media；服务端在所有写入和发布前递归重建白名单树，拒绝脚本/嵌入/未知节点、事件属性、内联样式、危险协议与外链/data 图片，图片仅接受站内 OSS Media ID，外链统一 `nofollow noopener`，React 渲染不使用原始 HTML。新增文章、专题、TLD、帮助详情和私有预览；公开读取双重要求 `_status=published` 与工作流 published，Proxy 前置门禁将流式 Not Found 收敛为真实 404，预览要求完成 TOTP 的内容角色并设置 `noindex/nofollow`、私有禁缓存；分类/标签匿名直接读取关闭。生成命名 migration、snapshot、类型与 import map，旧发布内容按来源安全回填。
- 验证证据：富文本与状态机单元测试覆盖完整迁移矩阵、来源、归档、修订、危险标签/属性/协议、外链图片、未知节点和链接 rel；PostgreSQL 集成覆盖六个 Collection 的 RBAC、版本/操作者、审计、公开边界与 publishing Job 幂等。最终 `make check` 通过生成类型/schema 漂移、空库及当前升级、D3 down/up 与旧内容回填、Nginx、lint、TypeScript strict、460 个单元测试、32 个 PostgreSQL/MinIO 集成测试、安全门禁、Next.js 生产构建和 linux/amd64 同镜像；Gitleaks 无泄漏，依赖审计为既有 2 low、2 moderate，无 high/critical。最终 `make test-e2e` 29/29 通过，覆盖内容编辑者登录、创建/审核/预览/发布/公开访问/下线/归档、四类详情、匿名预览与草稿 404、XSS 清洗和外链 rel。验证期间先发现 Next dev 会覆盖页面缓存头、流式 Not Found 返回 200 以及 Payload Button 缺少链接语义，均未放宽安全边界，分别以 Proxy 策略/发布门禁和原生安全链接修正；另一次 `make check` 被此前 E2E 的 fixture 价格快照污染，精确清理 9 条本地测试记录后同命令完整通过。
- 遗留风险/门槛：本切片只完成开发计划 7.2 前三项，不包含广告、仪表盘、订单、支付、实名、TLD/工具双向关联、完整 SEO/Redirect/Sitemap 或生产发布 Runbook。公共 Media 仍沿用 D0 已验证的 OSS 适配器，本轮不执行真实 OSS 写入；未部署、未修改共享 ECS、未调用真实 provider 或执行生产数据变更。依赖审计的 2 low、2 moderate 继续按现有门禁跟踪，生产上线条件不变。
- Git 状态：工作分支 `codex/d3-01-content-cms`；D3-01 实现、命名 migration、生成物、测试与本文档由该分支的 ready PR 指向 `main` 并交 Claude 审核。Codex 不批准、不合并、不部署；用户未跟踪的 `docs/planning/` 保持未修改且不进入提交。

### 2026-08-06（D3-02）

- 阶段状态：进行中；开发计划 7.2 第 4～6 项完成，D3 内容关联、SEO、动态 sitemap 与改址 301 切片达到完成定义；广告、后台仪表盘、表单和发布 Runbook 仍按后续独立切片推进。
- 完成内容：基于已合并 D3-01 的最新 `main` 建立 `codex/d3-02-content-seo-relations`。新增数据库固定种子的六个 `toolPages` 和内容侧 `relatedTools`、`relatedTldPages`，并在工具/TLD/文章/专题/帮助页两侧提供可维护的正向关系与分类型反向 Join；公开关系服务不信任已展开关系对象，而是按 ID 重新查询并双重要求 Payload `_status=published` 与业务 `workflowStatus=published`，草稿、下线、归档和无效 taxonomy 均不能通过关联旁路公开。工具关联目录合并读取并采用 30 秒短缓存、并发刷新去重、每类 200 条扫描与每工具每类 12 条展示上限，避免六个工具页重复制造查询风暴。复用 `@payloadcms/plugin-seo`，将文章、专题、TLD、帮助页、分类和标签纳入 SEO 标题、描述、Open Graph、同源 canonical 与默认可控 `noIndex`；新增分类/标签公开落地页，只有存在双重已发布文章时才通过 Proxy 发布门禁。sitemap 从静态列表升级为静态基线加动态已发布内容：每页读取 200 条、动态总量硬上限 5,000、taxonomy 关系扫描上限 10,000，每次请求动态生成并返回零 freshness/必须重新验证的缓存策略，只输出双重已发布、未 `noIndex` 且 canonical 指向自身的详情、有效分类和标签；跨页 canonical 由公开目标自身入图，数据库读取失败时安全回退静态基线。复用 D1-04 Redirects guard、审计与运行时缓存，把帮助、分类、标签和固定工具加入永久 301 引用目标；仍拒绝未发布目标、站外/协议相对 URL、保留路径、直接/多跳循环和超过最大链长。生成命名 migration `20260807_004430_d3_content_relations_seo`、snapshot、Payload 类型和工具种子，并扩展 migration round-trip verifier。
- 验证证据：单元测试新增 sitemap 分页/上限/发布与 noIndex 过滤、SEO/canonical、redirect 目标与循环/开放跳转、taxonomy 发布门禁和 RBAC；PostgreSQL 集成覆盖双向关系、匿名字段隐藏、草稿/下线关系过滤、taxonomy 可见性、SEO 字段与扩展 redirect 目标。最终 `make check` 通过生成类型/import map/schema 漂移、空库/遗留升级/D3-02 down-up migration、Nginx、lint、TypeScript strict、471 个单元测试、33 个 PostgreSQL/MinIO 集成测试、安全门禁、Next.js 生产构建和 linux/amd64 同镜像；Gitleaks 无泄漏，依赖审计维持既有 2 low、2 moderate，无 high/critical。最终 `make test-e2e` 30/30 通过，覆盖动态 SEO/canonical/noIndex、sitemap 仅收录已发布条目、分类/标签门禁、双向关系只展示已发布目标，以及扩展内容 301。完整 E2E 早期运行曾出现价格页/管理员/redirect/DNT 等不固定冷编译超时，五 workers 时进一步触发主机 `ERR_NETWORK_IO_SUSPENDED`；失败快照和定向场景确认非业务断言回归后，将 Playwright 固定为 2 workers、15 秒断言和 120 秒多步骤场景预算，最终整套稳定通过。随后 `make check` 首轮因 E2E 留存的更新价格快照影响 D2 固定时间“最新快照”断言而失败；价格集成 fixture 改为每次运行唯一 TLD/规则后，定向及最终完整门禁通过。发布前审查将 sitemap 从 5 分钟再验证进一步收紧为每次动态生成，并拒绝把跨页 canonical 写成潜在未发布 URL；对应 E2E 首轮因 Next 返回等价的 `public, max-age=0, must-revalidate` 而非 `no-store` 导致缓存头断言失败，改为断言任一零新鲜度/强制重验证策略后整套通过，未删除或改写失败事实。
- 遗留风险/门槛：关系展示采用有界目录扫描，超过展示/扫描上限的关联仍可在后台维护，但公开工具页只展示最近的有界集合；sitemap 5,000 条动态上限达到前需按既定分页结果扩展 sitemap index/分片，禁止直接取消上限。30 秒进程缓存与静态回退意味着内容发布或改址后可能短暂延迟展示，但不会把未发布内容公开。分类/标签当前只由已发布文章激活，专题/帮助/TLD 关系不单独激活 taxonomy。本切片未实现广告、仪表盘、表单、交易或部署，未修改共享 ECS，未调用真实西部数码、短信、OSS/KMS 或其他外部写接口；生产上线门槛不变。
- Git 状态：工作分支 `codex/d3-02-content-seo-relations` 基于已合并 D3-01 的最新 `origin/main`；D3-02 实现、migration、生成物、测试和本文档由该分支 ready PR 指向 `main` 并交 Claude 审核。Codex 不批准、不合并、不部署；用户未跟踪的 `docs/planning/` 保持未修改且不进入提交。

### 2026-08-06（D3-03）

- 阶段状态：进行中；开发计划 7.2 第 7～8 项完成，广告主、独立素材、广告位、排期、审核状态、受控跳转和显著商业标识达到完成定义；曝光/点击事件、到期自动下线、后台仪表盘及完整第 9 项回退策略仍按后续独立切片推进。
- 完成内容：基于已合并 D3-02 的最新 `origin/main` 建立 `codex/d3-03-advertising`。扩展广告主法定/联系/合同资料、精确外链主机白名单和四态生命周期；新增独立 `adMedia` Upload Collection、图片/文字素材、审核字段和五态生命周期；广告位固定页面类型、展示位置、设备范围与尺寸；排期使用服务端生成的随机 UUID 公开 ID、广告主/素材/广告位一致性、优先级、时间窗和六态生命周期。`ad_operator`/`system_admin` 可通过服务层状态机管理，`analyst` 只读且字段级隐藏联系人、合同、目标、白名单、审核/排期备注，公共 Local API 始终 `overrideAccess: false` 并只得到最小展示模型；创建、状态变更和删除复用统一脱敏审计。修复原 `targetUrl.startsWith('/')` 校验：内链直接复用 D1-04 `normalizeRedirectPath`，拒绝 `//evil.example`、`/\evil.example`、查询/片段与保留路径；外链只接受广告主预配置的精确主机、HTTPS、默认端口及无凭据/片段/动态占位符目标。新增 `/go/ad/<随机 UUID>`，不接收浏览器目标 URL，跳转时重新验证全关系/状态/时间/目标，忽略请求 query 且使用私有禁缓存和 origin referrer policy；D1-04 Redirects 同步保留 `/go`，不能截获受控广告跳转。工具与价格页仅在核心结果之后用独立 Suspense 槽读取；关闭、过期、素材/媒体无效或加载异常都折叠为空，不阻断工具。商业组件以独立 `aside`、显著“广告”和“不影响工具结果排序”说明与工具结果隔离，链接统一 `rel="sponsored nofollow noopener"`，外链安全新窗口。新增独立 OSS 广告素材前缀、命名 migration `20260807_025608_d3_advertising_controlled_delivery`、schema snapshot 与 Payload 类型；遗留公共 Media 素材安全迁入，旧外链及协议相对/反斜杠等不安全目标默认禁用。
- 验证证据：12 个新增单元测试覆盖复用 URL 规范化、协议相对/反斜杠/保留路径、外链白名单、随机标识、公开最小模型、加载失败折叠、商业标签/链接属性和跳转 query 隔离；2 个 PostgreSQL/MinIO 集成测试覆盖 ad operator 全状态发布、analyst 脱敏只读、审计不含 URL、禁用回收和非法目标拒绝；2 个新增 Playwright 场景覆盖广告位在核心结果后、显著标识、非工具结果、安全链接、无查询域名跳转及过期/任意目标关闭。迁移门禁新增空库、down/up、旧素材迁移、旧安全内链保留、旧 `//evil.example` 强制禁用、排期广告主和唯一 UUID 回填验证。最终 `make check` 通过生成类型/import map/schema 漂移、空库/遗留升级/D3-03 down-up migration、Nginx、lint、TypeScript strict、483 个单元测试、35 个 PostgreSQL/MinIO 集成测试、安全门禁、Next.js 生产构建和 linux/amd64 同镜像；Gitleaks 无泄漏，依赖审计维持既有 2 low、2 moderate，无 high/critical。最终 `make test-e2e` 32/32 通过。首轮 E2E 中广告场景复用 `wanmi.net` 先填充 provider cache，使既有“最新查询”断言看到合法缓存命中；改用隔离域名后消除跨文件污染。另一个既有移动内容断言因 Next 开发流式过渡的瞬时重复链接触发 strict-mode，收敛为检查首个同目标已发布链接后完整套件通过，未放宽广告、安全或发布边界。
- 遗留风险/门槛：本切片不记录广告请求、曝光、点击或转化，不实现自动到期状态写回、内容自然广告位、自有内容回退、运营仪表盘或真实广告投放；过期排期在读取/跳转时即时失效，但后台状态仍待后续 Job 收敛。外链必须先加入广告主精确主机白名单，子域不继承；白名单或素材变更后须重新经过审核状态。开发环境使用 MinIO 验证 Collection/访问路径，未执行真实 OSS 写入、部署、共享 ECS 修改、真实 provider/交易调用或生产数据变更；生产上线门槛不变。
- Git 状态：工作分支 `codex/d3-03-advertising` 基于已合并 D3-02 的最新 `origin/main`；D3-03 实现、migration、生成物、测试和本文档将由当前分支 ready PR 指向 `main` 并交 Claude 审核。Codex 不批准、不合并、不部署；用户未跟踪的 `docs/planning/` 保持未修改且不进入提交。

### 2026-08-07（D3-04）

- 阶段状态：进行中；开发计划 7.2 第 9～11 项完成，广告安全位置、第一方请求/曝光/点击/基础转化、到期自动下线和目标链接周期复检达到完成定义；第 12 项 Form Builder、第 13～14 项后台与聚合仪表盘继续留待后续独立切片。
- 完成内容：基于已合并 D3-03 的最新 `origin/main` 建立 `codex/d3-04-ad-events-safety`。公开读取强制 `tool → after_core_result`、`content → content_inline`、`tld → tld_inline`、`home → home_native` 精确位置映射，工具位继续位于完整结果之后，文章/专题/帮助/TLD 的自然广告位位于正文之后；读取、素材或健康检查失败默认折叠，不遮挡输入和核心结果。复用 D1-08 第一方入口，以 strict Zod 判别联合新增 `ad_requested`、进入视口的 `ad_served`、连续 1 秒达到 50% 可见的 `ad_viewable`、`ad_clicked` 和一次性站内 `landing_viewed` 转化；事件只含随机活动 UUID、广告位、页面类型和固定转化类型，继续使用 `credentials: omit`、origin-only referrer 与双端 DNT/GPC 门禁，短期 `sessionStorage` 归因键有版本、30 分钟失效且消费即删除，不含完整域名、账号、设备、会话或跨站 ID。新增每分钟运行的 Payload `background` 工作流，以全局 exclusive/supersedes key 和零自动重试调度维护；到期 `active/scheduled` 排期用 `updatedAt` 并发守卫收敛为 `ended`，重放无写入；批准素材首次及每 24 小时以 50 条批次、并发 4 复检，重新校验精确主机白名单，DNS 最多 16 个地址且任一受限地址即失败关闭，HTTPS 请求固定到已验证公网 IP 防重绑定、3 秒超时、HEAD 不支持时才使用有界 GET。素材只有 `reachable` 才可投放，目标/白名单异常分别进入封闭的 `unsafe/unreachable` 及失败枚举，素材变更重新置为 `pending`；维护审计只记目标记录 ID、操作和枚举状态，不记 URL。新增命名 migration `20260807_042030_d3_ad_events_maintenance`、snapshot、生成类型、事件/健康索引、Payload 调度元数据与完整往返验证；旧批准站内安全目标回填可达，外链默认待检。
- 验证证据：最终 `make check` 完整通过生成类型/import map/schema 漂移、空库/遗留升级及 D1-08/D3-03/D3-04 down-up 往返、Nginx、lint、TypeScript strict、494 个单元测试、36 个 PostgreSQL/MinIO 集成测试、依赖和秘密门禁、Next.js 生产构建及 linux/amd64 同镜像；依赖审计为既有 2 low、2 moderate，无 high/critical，Gitleaks 无泄漏。首轮定向集成测试暴露 Payload `beforeChange` 收到合并后的完整文档，维护字段守卫误拒绝正常更新；改为校验上下文中的精确预期结果和并发版本后通过。迁移往返首轮暴露 Job 枚举回滚的字面量重解析，改为显式文本比较后空库与往返通过。首次完整单测发现审计动作目录快照缺少 `advertising.maintenance`，补齐后 494/494 通过；一次完整集成运行的既有 Redirect `afterAll` 在并发负载下超出 10 秒但 36 个业务断言均已通过，单文件复跑 3/3、随后最终整套 36/36 通过。`make test-e2e` 首轮在 Next 开发冷编译/主机 I/O 高负载下为 29/32，新增广告两项已通过，失败为既有 Admin/CMS 页面或请求超时；Admin/CMS 定向复验及 Content 单 worker 分别通过，路由预热后最终原样 `make test-e2e` 32/32 通过。
- 遗留风险/门槛：P1 的基础转化仅定义为同站广告点击后 30 分钟内首次落地页浏览，不跨站跟踪、不给广告计费或交易提供权威依据；外部目标的可达性只代表周期检查时的 DNS/HTTPS 视角，实际点击仍由 D3-03 受控跳转实时重校验状态、白名单和排期。每轮素材复检有 50 条上限，积压会由后续每分钟 Job 有界清空；健康目标每 24 小时复检。未实现 Form Builder、广告报表/聚合仪表盘或真实广告投放，未部署、未修改共享 ECS、未发送真实 provider/资金/短信/域名写请求或修改生产数据；生产上线门槛不变。
- Git 状态：工作分支 `codex/d3-04-ad-events-safety` 基于已合并 D3-03 的最新 `origin/main`；D3-04 实现、migration、生成物、测试和本文档将由该分支 ready PR 指向 `main` 并交 Claude 审核。Codex 不批准、不合并、不部署；用户未跟踪的 `docs/planning/` 保持未修改且不进入提交。

### 2026-08-07（D3-05）

- 阶段状态：进行中；开发计划 7.2 第 12 项完成，Form Builder 联系、反馈和需求收集入口达到完成定义；后续运营后台、第一方事件聚合/基础仪表盘和内容模板 Runbook 继续留待独立切片。
- 完成内容：基于已合并 D3-04 的最新 `origin/main` 建立 `codex/d3-05-form-builder`。复用 D0 `@payloadcms/plugin-form-builder` 配置，通过 migration 建立 `contact`、`feedback`、`request` 三个唯一用途和精确字段矩阵，新增 `/contact`、`/feedback`、`/requests`、页脚入口、canonical/sitemap 及受控 `POST /api/v1/forms/submissions`。公开读取只返回安全表单视图，插件邮件、提交后跳转、订单/支付/退款/实名/上传字段和通用 REST/Local API 创建全部关闭。客户端复用 D1-02 六状态契约并显式呈现成功、失败、限流、空态和降级；提交前后双重清洗为 NFKC 纯文本，拒绝 HTML 和控制字符，限制长度/字段数/请求体，隐藏正文完整域名，移除相关页面 query/fragment，并拒绝域名伪装的请求 ID。联系方式原值仅 `system_admin` 可读，`ad_operator`/`analyst` 只读不含邮箱域名的掩码、清洗摘要和状态；运营摘要还会掩码正文中的邮箱、手机号与身份证样式值。客户端 IP 只转为加盐 HMAC，并按客户端/全局小时计数限流。状态只允许 `new → reviewed/closed` 与 `reviewed → closed`，仅系统管理员可操作，正文不可改写，变更写入脱敏审计，提交删除关闭。
- 验证证据：新增 7 个表单契约单元测试和 4 个 PostgreSQL 集成测试，覆盖批准矩阵、HTML/未知业务字段拒绝、完整域名与正文联系方式清理、通用创建关闭、运营角色脱敏只读、系统管理员状态机和审计、按客户端限流。命名 migration `20260807_061433_d3_form_builder_entries`、snapshot、Payload 类型与迁移 verifier 覆盖空库、升级、down-up、固定表单种子、唯一用途、无插件邮件/跳转、限流/状态索引及遗留 HTML/域名隐藏。最终 `make check` 完整通过：生成类型/import map/schema 漂移、全部 migration 往返、Nginx、lint、TypeScript strict、501/501 单元测试、40/40 PostgreSQL/MinIO 集成测试、依赖/秘密门禁、Next.js 生产构建和 linux/amd64 同镜像；依赖审计为既有 2 low、2 moderate，无 high/critical，Gitleaks 无泄漏。第一次完整 `make check` 仅因新增审计动作未同步既有动作目录快照而在 499/500 停止，补齐目录并新增正文敏感值回归后，同一原命令以 501/501 完整通过。最终原样 `make test-e2e` 33/33 通过，新增场景覆盖真实反馈提交、无 Cookie/origin-only referrer、请求 ID 回执，以及失败和限流状态；三类页面、canonical 与 sitemap 同步覆盖。
- 遗留风险/门槛：当前限流沿用数据库最近一小时计数，符合 P1 单 Web 进程与现阶段滥用防护思路；多 Web 实例前需采用数据库原子配额或共享限流协调，不能只依赖先计数后写入。正文域名隐藏是隐私最小化而非客服内容语义识别，系统管理员仍应按页面提示避免收集证件、支付凭据、OTP、Cookie 或其他秘密。后续运营后台、第一方事件聚合/基础仪表盘和内容模板 Runbook 均未实现；未部署、未发送真实邮件/provider/资金/短信/域名写请求或修改生产数据，生产上线门槛不变。
- Git 状态：工作分支 `codex/d3-05-form-builder` 基于已合并 D3-04 的最新 `origin/main`；D3-05 实现、migration、生成物、测试和本文档将由当前分支 ready PR 指向 `main` 并交 Claude 审核。Codex 不批准、不合并、不部署；用户未跟踪的 `docs/planning/` 保持未修改且不进入提交。

### 2026-08-07（D3-06）

- 阶段状态：进行中；开发计划 7.2 第 13～14 项完成，六个运营后台视图和复用 D2-11 聚合桶的基础运营仪表盘达到完成定义；第 15 项首批内容模板与发布 Runbook 仍留待后续独立切片。
- 完成内容：基于已合并 D3-05 的最新 `origin/main` 建立 `codex/d3-06-operations-dashboard`。在 D1-07 Payload Admin 导航骨架内追加工具状态、内容、广告、TLD/价格、反馈、审计和基础仪表盘入口，不另建导航；入口和直达页面均以 D1-06 角色矩阵服务端判定，未授权返回 404。所有运营读取通过当前管理员身份和 `overrideAccess: false` 执行，并以显式字段白名单生成只读视图模型。工具状态与仪表盘仅合并 D2-11 最近 24 小时 `toolObservabilityBuckets`，展示请求量、成功率、已有延迟分桶计算的 P50/P95、provider 错误、队列深度与拒绝量，不读取或展示完整查询域名、用户/会话/设备标识和原始事件。内容、广告、TLD/价格与反馈分别复用现有 Collection 和字段权限：`analyst` 只读聚合广告/工具与脱敏反馈，不能查看原始事件、审计和敏感字段；内容编辑只能查看内容/TLD 汇总，价格快照仅系统管理员可见。审计视图继续使用 D1-07 Collection 行级权限，`system_admin` 全量、`ad_operator` 仅本人、`analyst` 拒绝，并且视图不呈现 metadata。新增通用 access-enforced count helper、集中视图角色目录、响应式后台样式和生成 import map；未新增 Collection、指标或 migration。
- 验证证据：新增 Local API count 防护及运营视图角色矩阵单元测试，工具观测集成测试新增最近 24 小时多桶合并、精确分位数、provider 错误、队列状态和 analyst/system/非授权角色边界，E2E 新增 content editor、ad operator、analyst、system admin 四角色导航、直达拒绝、审计范围和价格敏感边界。最终 `make check` 完整通过生成类型/import map/schema 漂移、全部 migration 往返、Nginx、lint、TypeScript strict、507/507 单元测试、41/41 PostgreSQL/MinIO 集成测试、依赖/秘密门禁、Next.js 生产构建和 linux/amd64 同镜像；依赖审计为既有 2 low、2 moderate，无 high/critical，Gitleaks 无泄漏。完整 `make test-e2e` 首轮为 33/34，唯一失败是新增系统管理员用例复用了既有认证用例已消费的一次性恢复码；改用夹具预置的第二枚恢复码后，最终原样 `make test-e2e` 34/34 通过。
- 遗留风险/门槛：仪表盘当前固定最近 24 小时并使用 D2-11 小时桶，因此 P50/P95 代表合并后的有界直方图估算，队列状态显示窗口内最新与最大值；它不替代告警、原始事件调查或长期数仓。第 15 项内容模板和发布 Runbook、告警通知均未实现；本切片未部署、未修改共享 ECS、未调用真实 provider/资金/短信/域名写接口或修改生产数据，生产上线门槛不变。
- Git 状态：工作分支 `codex/d3-06-operations-dashboard` 基于已合并 D3-05 的最新 `origin/main`；D3-06 实现、测试和本文档将由当前分支 draft PR 指向 `main` 并交 Claude 审核。Codex 不批准、不合并、不部署；用户未跟踪的 `docs/planning/` 保持未修改且不进入提交。

### 2026-08-07（D3-07）

- 阶段状态：完成；开发计划 7.2 第 15 项首批内容模板与发布 Runbook 达到完成定义，D3 第 7.2 节 15 项全部完成。
- 完成内容：基于已合并 D3-06 的最新 `origin/main` 建立 `codex/d3-07-content-templates-runbook`。新增文章、专题、TLD 页面、帮助页四套可直接复制到 Payload 富文本编辑器的结构模板及统一索引，按现有 `title`、`slug`、`summary`、`content`、`source`、关系和 `meta.*` 字段区分 schema 硬要求与发布运营门槛；每套模板包含字段填法、正文骨架、作者/编辑/修订记录、提交审核清单，以及 SEO 标题/描述、Open Graph、canonical、`noIndex`、站内 Media 和原创/引用/转载/赞助四类来源与图片授权规范，不包含具体业务结论或数据库种子。新增内容发布 Runbook，精确覆盖自定义工作流按钮、`draft → in_review → published → unpublished → archived` 单向状态机、published 草稿修订、定时发布/直接改期/取消、匿名发布核验、下线与归档对 404、动态 sitemap、分类/标签、公开关系和 Redirect 引用的影响、引用型优先的受控 301 与禁止事项，并分别提供草稿、定时任务、错误修订、紧急下线和错误 301 回滚步骤；明确 `unpublished` 和 `archived` 不能重新发布，紧急下线后需建立替代记录并重新审核。
- 验证证据：四份模板的固定章节、`source`/`meta.*` 字段、五个工作流状态、原创/引用整理/转载/赞助内容格式和内部文档链接经只读脚本检查，Markdown Prettier 与 `git diff --check` 通过。最终原样 `make check` 以退出码 0 完整通过生成类型/import map/schema 漂移、空库/升级及全部 migration 往返、Nginx、lint、TypeScript strict、507/507 单元测试、41/41 PostgreSQL/MinIO 集成测试、依赖/秘密门禁、Next.js 生产构建和 linux/amd64 同镜像；依赖审计为既有 2 low、2 moderate，无 high/critical，Gitleaks 无泄漏。生产构建临时改写的 `apps/web/next-env.d.ts` 已恢复，最终无代码、Collection、schema、migration 或生成物变更。
- 遗留风险/门槛：模板只提供可执行结构和质量门槛，具体业务事实、首批正文、来源授权与持续更新仍由项目负责人负责；当前 schema 没有独立作者或修订原因字段，模板以正文署名/修订记录和公开 `source` 承载说明，实际操作者继续由版本、`revisionBy` 与审计追踪。`unpublished`/`archived` 的不可恢复边界已写入 Runbook，不得用数据库或通用 API 绕过。既有依赖审计 2 low、2 moderate 继续跟踪；未部署、未修改共享 ECS、未发布内容、未调用真实 provider/资金/短信/域名写接口或修改生产数据，生产上线门槛不变。
- Git 状态：工作分支 `codex/d3-07-content-templates-runbook` 基于已合并 D3-06 的最新 `origin/main`；实现提交 `db81f73` 已推送，GitHub ready PR #30 已创建并指向 `main`，交 Claude 审核。Codex 不批准、不合并、不部署；用户未跟踪的 `docs/planning/` 保持未修改且不进入提交。

## D4：账号与实名

### 2026-08-07（D4-01）

- 阶段状态：进行中；开发计划 8.2 第 1～4 项完成，短信 OTP、客户 Session、注销申请和客户/管理员认证隔离达到完成定义；实名模板与私有证件继续留给 D4-02 及后续切片。
- 完成内容：基于已合并 D3-07 的最新 `origin/main` 建立 `codex/d4-01-customer-auth-sms`，在 ADR-0003 原型上扩展而未重写认证体系。阿里云短信 provider 保留 deterministic mock，live 模式通过 Alibaba Cloud TypeScript SDK 发送验证码并查询回执，统一归类余额不足、模板未审、号码无效、限流和未知失败；受限 Collection 保存 provider 标识与投递状态，新增有界 `background` 回执核对 Job。手机号、IP、设备和全局四个限额分别使用 HMAC 标识及 PostgreSQL 原子冲突更新执行，不保存原始维度；OTP 只保存哈希、5 分钟过期、限制尝试次数并以条件更新一次性消费。沿用 `customers` 自定义 Payload Strategy 和随机 256-bit opaque Session，登录时轮换当前设备会话，补齐退出全部会话及确认式注销 endpoint；注销原子进入 `deletion_requested`、记录申请时间并撤销全部客户会话。`customers` 与 `admins` 保持两套 Collection、Strategy 和不同 Cookie，管理员端拒绝客户 Cookie，客户端也拒绝管理员 Cookie。新增命名 migration `20260807_095514_d4_customer_auth_sms`、schema snapshot、Payload 类型、迁移往返门禁和客户认证短信 Runbook。
- 验证证据：provider 单元测试覆盖 live 写门禁、mock 发送/回执、五类失败和无验证码泄漏；认证隔离单元测试覆盖 Collection、Strategy、Cookie 和关闭客户密码登录；PostgreSQL 集成测试覆盖四维独立配额、并发精确封顶、验证码重放/错误次数、回执同步、过期额度清理、全部会话撤销及注销；Playwright 通过真实 HTTP 覆盖统一防枚举响应、安全 Cookie、客户/管理员双向凭据隔离、退出全部会话、旧会话拒绝、注销和注销后统一响应。最终原样 `make check` 以退出码 0 完整通过生成类型/import map/schema 漂移、空库/升级及全部 migration 往返、Nginx、lint、TypeScript strict、520/520 单元测试、44/44 PostgreSQL/MinIO 集成测试、依赖/秘密门禁、Next.js 生产构建和 linux/amd64 同镜像；依赖审计为既有 2 low、2 moderate，无 high/critical，Gitleaks 无泄漏。最终原样 `make test-e2e` 35/35 通过。`ALLOW_REAL_PROVIDER_WRITES=false` 全程未改变，未发送真实短信。
- 遗留风险/门槛：短信 live 模式仍需在 D7/staging 使用已审批模板和最小权限 RAM 身份完成 contract test、回执时延与运营告警验证，且必须由项目负责人另行授权真实短信写操作。注销当前只立即停用账号和撤销全部会话；账号、实名文件及备份的 30 天清理由开发计划 8.2 第 11 项后续实现。实名模板、私有 OSS/KMS、证件访问与人工复核未实现；本切片未触碰订单、支付、部署、共享 ECS 或生产数据，生产上线门槛不变。
- Git 状态：工作分支 `codex/d4-01-customer-auth-sms` 基于已合并 D3-07 的最新 `origin/main`；实现提交 `1208546` 已推送，GitHub ready PR #31 已创建并指向 `main`，交 Claude 审核。Codex 不批准、不合并、不部署；用户未跟踪的 `docs/planning/` 保持未修改且不进入提交。

### 2026-08-07（D4-02）

- 阶段状态：已完成；开发计划 8.2 第 5、6、10 项完成，个人/组织实名模板、六态状态机、西部数码 mock/fixture 适配器和“仅 provider 确认通过方可注册”的服务端强约束达到完成定义；证件文件与清理流程继续留给 D4-03 及后续切片。
- 完成内容：基于已合并 D4-01 的最新 `origin/main` 建立 `codex/d4-02-realname-templates`。按仓库本地《西部数码业务 API 接口文档（v2）》的 `auditsub` 要求建立个人/组织字段模型，在独立 adapter 中映射 `c_regtype`、中英文所有者/联系人/地址、电话、邮箱、证件类型/号码和适用范围；只实现 deterministic mock/fixture，不提供 live transport。模板状态收敛为 `draft`、`pending_review`、`approved`、`rejected`、`manual_review`、`disabled`，非法迁移和绕过服务的 trusted update 均拒绝；合法变化沿用同一 `req` 追加脱敏审计。Customer 读取使用 owner 行级约束，敏感字段仅本人或系统管理员可读，后台默认列表不含证件号、电话、邮箱或 provider 标识。provider 创建/查询失败和未知状态进入 `manual_review + unknown`，不会误标通过；注册可用性服务同时要求本人归属、业务状态 `approved`、provider 状态 `approved`、provider 模板 ID 与确认时间，其他全部拒绝。新增命名 migration `20260807_114644_d4_realname_templates`、schema snapshot、Payload 类型和迁移往返门禁；历史占位模板升级时统一安全停用并清除旧任意失败文本。
- 验证证据：adapter 单元测试覆盖个人/组织与手机/座机字段映射、无网络调用、provider 不可用和后台安全列；状态机单元测试覆盖全部合法迁移及非法反向/跳跃迁移。PostgreSQL 集成测试覆盖本人/他人行级隔离、直接伪造通过拒绝、审核中/未通过/待人工/已停用不可注册、仅 provider 确认通过可注册、客户不能主动同步 provider 状态、provider 不可用 fail-closed，以及每次状态变化审计不含完整手机号或证件号。最终原样 `make check` 以退出码 0 完整通过生成物/schema 漂移、空库/历史升级及全部 migration 往返、Nginx、lint、TypeScript strict、524/524 单元测试、46/46 PostgreSQL/MinIO 集成测试、依赖/秘密门禁、Next.js 生产构建和 linux/amd64 同镜像；依赖审计为既有 2 low、2 moderate，无 high/critical，Gitleaks 无泄漏。最终原样 `make test-e2e` 35/35 通过。未调用真实西部数码接口。
- 遗留风险/门槛：证件文件上传、`ali-oss`、KMS 信封加密、恶意文件检查、短时签名访问、证件查看/下载/提交/删除审计属于第 7～9 项，留给 D4-03；30 天清理任务和项目负责人人工复核/修改重提仍未实现。西部数码实名 live transport、写权限与字段 contract test 仍须书面确认和另行授权；本切片未触碰订单、支付、部署、共享 ECS 或生产数据，生产上线门槛不变。
- Git 状态：工作分支 `codex/d4-02-realname-templates` 基于已合并 D4-01 的最新 `origin/main`；实现提交 `0aa9d08` 已推送，GitHub ready PR #32 已创建并指向 `main`，交 Claude 审核。Codex 不批准、不合并、不部署；用户未跟踪的 `docs/planning/` 保持未修改且不进入提交。

### 2026-08-07（D4-03）

- 阶段状态：已完成；开发计划 8.2 第 7～9 项完成，私有证件上传、内容检查、KMS 信封加密、短时访问和全生命周期审计达到完成定义；30 天清理任务继续留给 D4-04。
- 完成内容：在 ADR-0005 的公共/私有存储分离架构上扩展现有 `oss-realname` 与 `kms` provider，没有建立平行存储系统。上传仅接受经魔术字节与结构验证的 JPEG、PNG、PDF，在配置上限内检查图片解码/尾随载荷、可执行文件头、EICAR 与 PDF 主动内容，并重新编码图片以清除元数据和附加载荷；客户端扩展名和 `Content-Type` 均不可信。每个对象使用 KMS `GenerateDataKey` 生成独立 AES-256 数据密钥，以 AES-256-GCM 和认证头加密，密文、加密数据密钥及完整性元数据封装进私有对象，明文数据密钥在使用后清零。对象键使用 UUID 加额外随机字节且被 provider 强制约束在独立私有前缀；live OSS/KMS factory 在 `ALLOW_REAL_PROVIDER_WRITES=false` 时于构造客户端前拒绝。新增 customer 与 system_admin 分离的短时签名票据，包含 actor、action、nonce 和到期时间，最长 120 秒，URL 不暴露对象键；兑换时重新核对 customer 归属或管理员密码/TOTP Session，查看与下载动作不可互换。上传、查看、下载、提交、删除均写入脱敏审计，删除期间立即阻断访问并具备失败回滚。隐藏 Payload Admin 证件 Collection，字段级限制保护存储与加密元数据；日志、错误和审计 sanitizer 禁止证件内容、对象键、数据密钥、IV 和认证标签。新增 migration `20260807_125811_d4_private_realname_documents`、历史占位记录的 fail-closed 升级、运维 Runbook、单元/集成/E2E 覆盖。
- 验证证据：最终原样 `make check` 通过：生成类型/import map/schema 漂移为零，D4-03 空库、历史记录安全升级与全部 migration 往返通过，Nginx 7 组测试通过，lint 与 TypeScript strict 通过，单元测试 529/529、PostgreSQL/MinIO 集成测试 47/47，通过依赖和秘密门禁、Next.js 生产构建及 linux/amd64 同镜像构建；依赖审计维持既有 2 low、2 moderate，无 high/critical，Gitleaks 无泄漏。最终原样 `make test-e2e` 36/36 通过，覆盖真实 OTP Session 下伪造扩展名/MIME 上传、短时查看、恶意 PDF 通用错误、提交、删除和旧票据拒绝。全部测试维持 `ALLOW_REAL_PROVIDER_WRITES=false`，没有连接真实 OSS/KMS。
- 遗留风险/门槛：第 8.2 节第 11 项 30 天账号、证件和备份清理未实现，留给 D4-04。live OSS/KMS 仍需在 staging 以专用 RAM 身份验证仅限私有前缀的 Put/Get/Delete 与指定 KMS key 的 GenerateDataKey/Decrypt，并完成轮换、告警、故障恢复和删除/误删恢复演练；这些真实 provider 操作必须另行授权。本切片未触碰订单、支付、部署、共享 ECS 或生产数据，生产上线门槛不变。
- Git 状态：工作分支 `codex/d4-03-private-documents` 基于已合并 D4-02 的最新 `origin/main`；实现提交 `ea501fe` 已推送，GitHub ready PR #33 已创建并指向 `main`，交 Claude 审核。Codex 不批准、不合并、不部署；用户未跟踪的 `docs/planning/` 保持未修改且不进入提交。

### 2026-08-07（D4-04）

- 阶段状态：完成；开发计划 8.2 第 11～12 项达到完成定义，D4 的 12 项任务全部完成。
- 完成内容：模板删除端点和账号注销事务均先立即把模板置为 `disabled`，以同次状态变化的 `disabledAt` 为可审计倒计时起点并固化 `cleanupDueAt = disabledAt + 30 天`。新增独立 `background` 队列 `realnameCleanup` Payload Job，使用排他并发键按到期时间扫描；逐个实际删除私有 OSS 主对象和全部备份对象，每个对象成功后写完成标记，使 Worker 重启和任务重放不会重复发出删除或重复完成审计。对象全部删除后物理删除证件数据库行；模板无历史订单引用时物理删除，存在既有必需订单外键时不触碰订单，只保留清空姓名、证件、联系方式和 provider 标识的永久 disabled 审计骨架。被拒绝模板允许修改后回到草稿并重新提交；provider 明确拒绝为 `rejected`，抛错、不可用或未知结果均 fail-closed 进入 `manual_review` 并建立一条开放复核记录。人工出口仅允许密码/TOTP Session 有效的 `system_admin`，且备注、外部证据来源、引用和观察时间缺一不可；复核结果与记录关闭使用同一数据库事务。新增 customer 删除/修改 endpoint、admin 复核 endpoint、两个命名 migration、类型生成、迁移往返检查和实名生命周期 Runbook。
- 验证证据：单元测试覆盖精确 30 天计算、状态/审计目录和 provider 拒绝/未知映射；PostgreSQL/MinIO 集成测试覆盖模板删除及账号注销立即停用、到期前不清理、主对象与备份对象实际删除、证件行删除、Job 重放无重复删除/审计、拒绝模板修改重提、provider 不可用进入人工复核、非管理员与缺证据拒绝、system_admin 带证据恢复并关闭复核记录。最终原样 `make check` 以退出码 0 完整通过生成类型/import map/schema 漂移、空库/历史升级及全部 migration 往返、Nginx、lint、TypeScript strict、530/530 单元测试、48/48 PostgreSQL/MinIO 集成测试、依赖/秘密门禁、Next.js 生产构建和 linux/amd64 同镜像；依赖审计为既有 2 low、2 moderate，无 high/critical，Gitleaks 无泄漏。最终原样 `make test-e2e` 36/36 通过。测试全程使用 mock provider，未执行真实 OSS/KMS/西部数码写操作。
- 遗留风险/门槛：历史订单必需外键使被引用模板不能物理删行；当前实现按不修改订单的边界保留完全去标识、不可恢复和不可注册的审计骨架，证件行与 OSS 主/备份对象仍全部实际删除。live OSS/KMS 和西部数码实名 transport 仍须在 D7/staging 使用最小权限身份完成 contract test、备份恢复、密钥轮换、告警及故障恢复演练，并由项目负责人另行授权；本切片未改变订单状态或数据，未触碰支付、部署、共享 ECS 或生产数据，生产上线门槛不变。
- Git 状态：工作分支 `codex/d4-04-realname-lifecycle` 基于已合并 D4-03 的最新 `origin/main`；本条随 D4-04 实现提交推送并创建指向 `main` 的草稿 PR，交 Claude 审核。Codex 不批准、不合并、不部署；用户未跟踪的 `docs/planning/` 保持未修改且不进入提交。

## D5：报价、订单与微信支付

### 2026-08-07（D5-01）

- 阶段状态：进行中；开发计划 9.2 第 2 项达到完成定义。第 3 项所需的报价归属、有效期和完整性校验入口已建立，但实际订单创建与重新验证留给 D5-02，因此本次不提前勾选；第 1 项后台可发布加价规则及审计仍未完成。
- 完成内容：基于已合并 D4-04 的最新 `origin/main` 建立 `codex/d5-01-customer-quotes`。在 D2-07 已有整数分、BigInt、舍入模式、可复现计算链和 `priceSnapshots` 上扩展客户报价服务，没有另建价格算法。认证客户通过严格 4 KiB JSON 和共享 Zod 的 `POST /api/v1/quotes` 提交规范化域名与 1～10 年年限；服务复用既有 TLD 支持和加价规则门禁，查询普通域名可售性与价格后保存客户、域名、TLD、年限、币种、上游注册/续费/总成本、完整加价与舍入快照、最终注册/续费/总价、provider 请求和观察时间、来源价格快照引用及计算哈希、报价完整性哈希、创建时间和精确 5 分钟失效时间。响应只公开安全价格字段和不透明报价引用，不泄露上游成本、规则或 provider 请求。报价 Collection 禁止通用创建、修改和删除，客户读取按本人行级隔离；下单复用服务先使用 `user + overrideAccess: false` 验证归属，再校验过期和完整性，他人、过期或被篡改报价全部 fail-closed。未配置规则、未支持 TLD、不可注册及溢价域名不创建报价，且未配置规则在调用 provider 前即关闭。新增命名 migration、Payload 类型和迁移往返门禁；旧占位报价保留但强制过期并标记为不可受信，不能成为订单依据。
- 验证证据：单元测试覆盖 3 年上游/最终整数总价、精确 300000 毫秒有效期、规则及来源快照、未配置或未支持 TLD 零 provider 调用、不可注册、溢价、限流、他人访问、过期和完整性篡改；PostgreSQL 集成测试覆盖完整快照落库、来源 `priceSnapshots`、本人安全字段读取、他人不可读取/使用、通用写入拒绝及有效期内复用；Playwright 通过真实 OTP Session 覆盖匿名拒绝、3 年报价、精确有效期和敏感字段不外泄。最终原样 `make check` 以退出码 0 完整通过生成物/schema 漂移、空库/历史升级及全部 migration 往返、Nginx、lint、TypeScript strict、537/537 单元测试、49/49 PostgreSQL/MinIO 集成测试、依赖/秘密门禁、Next.js 生产构建和 linux/amd64 同镜像；依赖审计维持既有 2 low、2 moderate，无 high/critical，Gitleaks 无泄漏。最终原样 `make test-e2e` 36/36 通过。
- 遗留风险/门槛：当前报价继续使用明确标识的本地西部数码 fixture，不代表实时可购买承诺；真实只读 transport、字段 contract test、限流和运营告警仍须另行授权验证。第 9.2 节第 1 项的后台 TLD 加价规则发布与审计未实现；D5-02 必须在订单创建事务中调用报价复用服务并重新核验 TLD、实名模板、域名状态和价格条件，不能只信任报价引用。本切片不创建订单、不实现状态机或支付，不触碰实名、部署、共享 ECS 或生产数据，也未调用任何真实 provider 写接口。
- Git 状态：工作分支 `codex/d5-01-customer-quotes` 基于已合并 D4-04 的最新 `origin/main`；本条随 D5-01 实现提交推送并创建指向 `main` 的 ready PR，交 Claude 审核。Codex 不批准、不合并、不部署；用户未跟踪的 `docs/planning/` 保持未修改且不进入提交。

### 2026-08-07（D5-02）

- 阶段状态：进行中；开发计划 9.2 第 3～4 项达到完成定义，订单创建、完整重新验证、`pending_payment` 起始状态、ADR-0004 状态矩阵和追加式 `orderEvents` 已闭环。第 1 项后台可发布加价规则及审计仍未完成；微信支付、退款、对账和履约不在本切片。
- 完成内容：基于已合并 D5-01 的最新 `origin/main` 建立 `codex/d5-02-orders`。新增严格 4 KiB JSON 的认证客户 `POST /api/v1/orders`，请求只接受不透明报价引用与实名模板 ID，Zod 严格拒绝客户端传入域名、金额、币种、年限或状态。commerce 订单创建服务在同一 Payload 事务中先调用 D5-01 `getUsableCustomerQuote` 核验客户归属、有效期和完整性哈希，再实际调用 D4-02 `assertRealnameTemplateUsableForRegistration` 核验模板归属、`approved` 状态、provider 批准、模板标识和确认时间；随后重新确认支持 TLD 与当前已配置规则，要求当前规则和报价规则完全相同，以当前规则重算注册、续费、上游总成本和用户多年总价并与报价一致，最后对同一规范化域名重新查询可售状态，只有仍为普通非溢价可注册时才写订单。成功订单绑定客户、报价、实名模板、域名及服务端金额，币种固定 `CNY`、状态固定 `pending_payment`，保存包含完整计算、报价完整性、来源快照及下单时可售查询证据的 `quoteSnapshot`，并追加一条 `order.created` 事件；任一门禁失败回滚，不残留订单或事件。客户可读订单继续隐藏内部快照，Orders/OrderEvents 通用 create/update/delete 继续全部关闭，浏览器和 Admin 均不能直接改状态。没有重写 D0 状态机：现有 `transitionOrder` 仍是唯一迁移入口，保留事务、CAS 并发保护和追加事件，测试按 ADR-0004 显式锁定完整合法矩阵、拒绝未定义迁移并校验 `manual_review` 出口证据。现有 schema 已覆盖所需字段，本切片没有新增 migration 或生成类型变化。
- 验证证据：单元测试覆盖当前规则缺失/变化、金额重放不一致、完整 ADR-0004 矩阵、未定义迁移拒绝、严格订单 body、认证先行和客户端金额/域名/状态注入拒绝；PostgreSQL/MinIO 集成测试使用真实 D4 模板提交/批准流程和 D5 报价落库，覆盖原子创建一条待支付订单、客户/报价/模板绑定、金额与快照一致、首次事件追加、客户快照脱敏、通用状态更新拒绝、草稿实名拒绝、域名后来不可售拒绝及失败无残留。最终原样 `make check` 以退出码 0 完整通过生成物/schema 漂移、空库/历史升级及全部 migration 往返、Nginx、lint、TypeScript strict、544/544 单元测试、50/50 PostgreSQL/MinIO 集成测试、依赖/秘密门禁、Next.js 生产构建和 linux/amd64 同镜像；最终原样 `make test-e2e` 36/36 通过。安全门禁同时发现并修复 `nanoid` 可升级高危项；`image-size@2.0.2` 的两个无限循环高危项尚无上游修复版本，已对 ICNS、JXL/HEIF 零尺寸 box 添加最小本地补丁和不会挂起的恶意输入回归测试，audit 仅精确忽略这两个已修补 GHSA，Gitleaks 无泄漏。
- 遗留风险/门槛：当前下单重新查询继续使用明确标识的本地西部数码 fixture，不构成实时可购买承诺；真实只读 transport、字段 contract test、限流、运营告警及西部数码写接口书面确认仍须另行授权验证。开发计划 9.2 第 1 项后台可发布加价规则与审计仍未实现；订单当前只停在 `pending_payment`，没有微信支付、支付通知、退款、对账、履约或域名资产。依赖本地补丁须在上游发布修复版本后移除并重新跑完整安全门禁。未部署、未修改共享 ECS、未执行真实资金、域名、实名或其他外部写操作，生产上线门槛不变。
- Git 状态：工作分支 `codex/d5-02-orders` 基于已合并 D5-01 的最新 `origin/main`；本条随 D5-02 实现提交推送并创建指向 `main` 的 PR，交 Claude 审核。Codex 不批准、不合并、不部署；用户未跟踪的 `docs/planning/` 保持未修改且不进入提交。

### 2026-08-07（D5-03）

- 阶段状态：进行中；开发计划 9.2 的 Native/H5 服务端下单、支付前报价有效期、通知验签/幂等入库/防重放/主动查单达到完成定义。原合并任务已拆分，前端二维码、H5 跳转/返回、超时关单、退款、补单/对账和真实商户联调仍未完成。
- 完成内容：基于已合并 D5-02 的最新 `origin/main` 建立 `codex/d5-03-wechat-payments`。新增可注入 WeChat Pay API v3 adapter，实现 Native/H5 下单、RSA-SHA256 请求签名、微信平台响应/通知验签、AES-256-GCM 解密与按商户单号查单；生产路径不内置真实 transport，运行时 fixture 密钥在内存生成。新增认证客户支付创建/查询 endpoint 和微信通知 endpoint；发起支付前重新校验订单归属、`pending_payment` 及服务端报价失效时间，先入库唯一商户订单号再调 provider。通知内容只在验签成功后成为查单线索，且只有经平台签名的查单结果与服务端商户/应用/订单/交易/币种/金额全部一致时，才在同一事务内写通知、到账时间并复用 `transitionOrder` 迁移 `pending_payment → paid`。金额或标识不一致、查单未知、取消后迟到支付进入人工复核；明确未支付保持待支付，伪造通知不信任也不迁移。数据库对订单商户号、通知 ID、通知商户号和微信交易号建立四项唯一索引；重放通过唯一约束、事务和已确认状态收敛，只迁移一次。通知仅持久化验签/确认结果、安全标识、金额、接收/到账时间与 SHA-256 摘要，不保存完整报文。新增命名 migration、Payload 生成类型、共享 Zod 契约和迁移往返/历史回填门禁；按当前资金安全指令将 `pending_payment → manual_review` 仅增补到技术矩阵和 ADR-0004，其他冻结状态、退款及上线门槛不变。
- 验证证据：adapter 单元测试覆盖 Native/H5、经签名查单、RSA+AES 通知、签名篡改、报价过期与客户 IP 失败关闭；PostgreSQL/MinIO 集成测试覆盖服务端查单确认到账、重放仅一次迁移、四项数据库唯一约束、伪造通知无可信标识/金额、金额不一致和查单未知进入人工复核、`NOTPAY` 继续待支付。最终原样 `make check` 以退出码 0 通过生成物/schema 漂移、空库/历史升级与全部 migration 往返、Nginx、lint、TypeScript strict、547/547 单元测试、54/54 PostgreSQL/MinIO 集成测试、依赖/秘密门禁、Next.js 生产构建和 linux/amd64 同镜像；新增 D5-03 migration 检查后再单独执行 `make verify-migrations` 通过空库、历史通知回填与 down/up 往返。最终原样 `make test-e2e` 36/36 通过。`ALLOW_REAL_PROVIDER_WRITES=false`，全程仅使用 mock/fixture，未连接真实微信商户号或发起资金请求。
- 遗留风险/门槛：本切片按用户边界不实现退款、履约、超时关单、支付补单/对账工具、桌面二维码或 H5 前端流程；真实商户证书/平台公钥轮换、官方 fixture contract test、测试商户小额联调、支付告警、通知重放演练与三方对账仍须在后续切片/生产准入中通过。生产上线、部署和真实资金权限仍未授权；不记录支付密钥、证书或完整通知报文。
- Git 状态：工作分支 `codex/d5-03-wechat-payments` 基于已合并 D5-02 的最新 `origin/main`；本条随 D5-03 实现提交推送并创建指向 `main` 的 ready PR，交 Claude 审核。Codex 不批准、不合并、不部署；用户未跟踪的 `docs/planning/` 保持未修改且不进入提交。

### 2026-08-07（D5-04）

- 阶段状态：进行中；开发计划 9.2 的退款任务/查询/失败告警和微信支付、内部订单、西部数码成本对账数据结构达到完成定义。D5 其他未勾选项保持不变，履约仍属于 D6。
- 完成内容：基于已合并 D5-03 与批准文档基线的最新 `main` 建立 `codex/d5-04-wechat-refunds-reconciliation`。扩展现有可注入 WeChat Pay API v3 adapter，实现退款创建、按退款号查询、平台签名响应、RSA 验签、AES-256-GCM 退款成功通知解密及运行时内存 fixture；新增独立 `commerce` 队列 `wechatRefund`，重试为 0，按退款 ID 使用并发键。注册明确失败入口只接受 `paid`/`fulfilling`，注册成功 `succeeded` 在 provider 调用前硬拒绝；自动退款固定为原路全额，除核对订单金额外还必须找到 D5-03 已确认支付通知并再次核对商户订单号、微信交易号、币种和整数分金额。退款订单状态只通过既有 `transitionOrder` 走 `refund_pending → refunding → refunded`；退款请求发出后超时/未知会固化 `providerOperations=unknown` 并进入人工复核，后续重放只查询，不再发起退款。明确失败、争议、余额不足、查询未知、通知金额/标识不一致都建立开放人工复核并输出脱敏告警。退款通知 endpoint 在解密前验签，伪造通知不保存可信金额/标识；通知号唯一、报文摘要、事务和主动查询共同实现幂等、防重放。数据库保留并验证退款号/微信退款 ID 唯一约束，新增一订单一退款、退款通知 ID 和对账键唯一约束。对账按 `wechat_funds`、`westdigital_prepaid`、`internal_orders` 三类 ledger 分开保存；西部余额按本地 API 文档 `checkbalance` 的可用余额/冻结金额语义使用 fixture 输入，绝不调用真实接口。微信、内部订单和西部预充值的差异只追加 `difference` 记录，三方关联明确保存 `correctionApplied: false`，没有自动改订单、退款或余额的代码路径。新增命名 migration、Payload 类型、空库/历史回填/down-up 往返检查和资金安全单元/集成覆盖。
- 验证证据：WeChat adapter 单元测试覆盖退款创建、查询、平台签名响应、退款通知验签/AES 解密及签名篡改；PostgreSQL/MinIO 集成测试覆盖确认支付证据门禁、唯一退款、数据库退款号唯一约束、成功订单禁退、精确全额、`refund_pending → refunding → refunded`、首次提交后仅查询、未知/余额不足转人工、伪造/金额不一致/成功退款通知、通知重放，以及微信资金和西部预充值分账、三方差异只记录不修正。最终原样 `make check` 以退出码 0 完整通过生成类型/import map/schema 漂移、D5-04 空库/历史对账回填/全部 migration 往返、Nginx、lint、TypeScript strict、548/548 单元测试、59/59 PostgreSQL/MinIO 集成测试、依赖与秘密门禁、Next.js 生产构建及 linux/amd64 同镜像；最终原样 `make test-e2e` 36/36 通过。
- 遗留风险/门槛：全程保持 `ALLOW_REAL_PROVIDER_WRITES=false`，只使用运行时生成密钥的 mock/fixture；未连接真实微信商户、未执行真实资金请求、未查询真实西部数码余额，也未实现 D6 履约。微信官方 fixture contract test、测试商户小额退款、证书/公钥轮换、账单下载、退款/对账运营告警演练及真实西部余额 contract test 仍需在 D7/staging 获得另行授权后完成。第 9.2 节的前端支付流程、支付超时关单、支付通知补单工具、特殊退款/发票备注与人工操作审计仍未完成；生产上线门槛不变。
- Git 状态：工作分支 `codex/d5-04-wechat-refunds-reconciliation` 基于已合并 D5-03 与批准文档基线的最新 `main`；本条随 D5-04 实现提交推送并创建指向 `main` 的 ready PR，交 Claude 审核。Codex 不批准、不合并、不部署；用户未跟踪的 `docs/planning/` 保持未修改且不进入提交。

### 2026-08-08（集成测试隔离性加固）

- 阶段状态：完成；这是独立的纯测试加固切片，不对应开发计划编号，未勾选或修改开发计划，未增加功能、未修改 `src` 产品代码、schema、migration 或生成类型。
- 完成内容：价格快照并发去重用例改为同时按本用例的 `tld`、`calculationHash` 和 `snapshotRef` 查询，只验证本次 fixture 的唯一行和最新匹配结果，不再假设价格历史表为空。D0 重复 commerce Job 用例记录本次入队的两个 Job ID，并通过 `runByID` 定向执行，保留 provider operation 唯一、事件追加、订单和域名状态等原断言，避免陈旧未完成 Job 占用队列 `limit`。同时审计其余集成测试：`auditLogs` 已按 trace/target 限定，`smsChallenges` 核心断言使用唯一 challenge ID，`sms_rate_limits` 配额断言使用唯一手机号桶；对发现的同类风险，将 first-party event 与 Form Builder 的拒绝落库断言限定到随机 TLD/本次 trace ID，将工具观测桶限定到精确 fixture bucket key 并在用例前只清理这些键，将退款测试的 Job 清理限定到本次 trace 前缀，未清除其他测试或历史数据。
- 断言强度：没有删除或放宽业务断言。价格快照从全表偶然计数收紧为本 fixture 的复合身份唯一性；拒绝落库用例从“全表总数前后相等”收紧为本次被拒绝标识在前后都精确为 0。短信回执的全局 `checked: 1, delivered: 1` 会被其他符合扫描条件的历史 challenge 干扰，现改为要求本轮所有被扫描记录均投递成功、失败数精确为 0，并继续单独确认目标 challenge 已投递，覆盖集合更大且不弱于原成功不变量。工具桶预清理和退款 Job 清理均只触及测试自身可识别 fixture。
- 验证证据：聚焦运行 6 个改动文件共 27/27 用例通过。随后在同一数据库上不清库、不重置，连续运行两次原样 `make check`，两次均以退出码 0 完整通过 migration/生成物/schema 漂移、Nginx、lint、TypeScript strict、548/548 单元测试、59/59 PostgreSQL/MinIO 集成测试、依赖与秘密门禁、Next.js 生产构建及 linux/amd64 镜像构建；第二次验证了第一次遗留数据存在时仍保持全绿。
- 遗留风险/门槛：本次仅排除已识别的共享累积表初始状态假设；未来新增集成测试仍应使用唯一 fixture 业务键、定向 Job ID 和范围化清理。没有执行真实 provider、资金、短信或域名写请求，`ALLOW_REAL_PROVIDER_WRITES=false` 边界及生产上线门槛不变。
- Git 状态：工作分支 `codex/test-isolation-hardening` 基于已合并 D5-04 的最新 `main`；本条与测试改动将提交、推送并创建指向 `main` 的 ready PR，交 Claude 审核。Codex 不批准、不合并；用户未跟踪的 `docs/planning/` 保持未修改且不进入提交。

### 2026-08-08（D5-05）

- 阶段状态：完成；开发计划 9.2 第 1 项达到完成定义，D5 唯一未完成的价格配置项闭环。桌面二维码、移动 H5 前端、超时关单、服务端状态展示、通知补单及其他未勾选项保持不变。
- 完成内容：基于最新 `main` 建立 `codex/d5-05-price-rules`，启用 D0 已有 `priceRules` Collection，不新建平行模型。Collection 的创建、修改、启用、停用和删除只向 `system_admin` 开放；公开价格查询、认证客户报价和订单当前规则复核统一通过价格规则服务读取已启用 Collection，硬编码规则退出生产路径，fixture 只保留为测试显式注入。规则写入前规范化 TLD，并严格拒绝负数、非安全整数、小数基点以及 fixed/percentage 模式与金额字段不匹配；金额继续使用整数分，比例加价继续复用 BigInt half-up 算法。新增只读 `effectiveAt` 生效时间及命名 migration `20260808_053208_d5_price_rules`，历史规则以原更新时间安全回填。新增、修改、启用、停用和删除复用 D1-07 `recordAuditEvent`，在同一 Payload 请求事务中记录 TLD、模式、变更前后整数金额或基点、操作者和生效时间，没有另建审计系统。
- 验证证据：新增单元与 PostgreSQL 集成测试，覆盖角色写权限、非法规则写入拒绝、固定/比例规则落库、启停/删除和统一审计、只读取启用规则、缺失/停用规则在 provider 调用前关闭购买。关键快照不变量已验证：规则修改后，新报价使用新 revision；旧报价内完整规则副本、计算哈希和完整性哈希不变，并可按原快照复现原整数价格；订单创建仍按当前 Collection 规则重新校验，因此不会让旧报价绕过价格变化门禁。迁移 verifier 覆盖空库、历史回填、来源枚举、nullable fixed 金额、索引和 down/up 往返。最终在同一数据库状态上连续两次原样 `make check` 均以退出码 0 完整通过生成物/schema 漂移、全部 migration 往返、Nginx、lint、TypeScript strict、550/550 单元测试、61/61 PostgreSQL/MinIO 集成测试、依赖/秘密门禁、Next.js 生产构建和 linux/amd64 同镜像；最终原样 `make test-e2e` 36/36 通过。
- 遗留风险/门槛：migration 不会自动发布任何生产规则；部署后须由授权 `system_admin` 按已确认价格发布，未配置或停用 TLD 会继续 fail-closed。真实西部数码只读价格 transport、运营告警和发布操作演练仍需在后续 staging 获得授权后完成。本切片未改订单状态机、支付或退款，未部署、未修改共享 ECS、未执行真实资金、短信、域名或其他 provider 写操作，生产上线门槛不变。
- Git 状态：工作分支 `codex/d5-05-price-rules` 基于已合并 D5-04 与测试隔离加固后的最新 `main`；本条随 D5-05 实现、migration、生成物、测试和文档提交推送，并创建指向 `main` 的 ready PR 交 Claude 审核。Codex 不批准、不合并、不部署；用户未跟踪的 `docs/planning/` 保持未修改且不进入提交。

### 2026-08-08（D5-06）

- 阶段状态：完成；开发计划 9.2 的桌面二维码、移动 H5 跳转/返回/服务端状态、前端服务端状态唯一判定和支付超时关单四项达到完成定义。通知重放补单、特殊退款与发票审计仍留给 D5-07，D5 整体继续进行中。
- 完成内容：新增订单支付页与返回落地页。桌面 Native 流程将服务端 `code_url` 动态生成二维码，显示失效时间、过期提示和重新获取入口；移动 H5 流程只跳转服务端返回的 `h5_url`，并附加站内返回地址。扫码、跳转和返回均不会改变前端支付结论，页面只按 D1-02 六状态契约轮询同源接口并以服务端订单状态展示结果。状态接口使用认证 customer、`user` 与 `overrideAccess: false` 限定订单归属，以订单行 `paymentStatusPolledAt` 做数据库原子 3 秒限频，禁用缓存且不返回额外支付凭据。新增 WeChat Pay API v3 关单适配器及 `commerce` 队列独占 `paymentTimeoutClose` workflow，每 30 秒自限定扫描最多 100 个到期的 `pending_payment` 订单：先主动查单；已支付复用 D5-03 逻辑，未知状态不关单；明确未支付时调用关单并再次查单，只有平台确认 `CLOSED` 才复用既有 `transitionOrder` 迁移 `pending_payment → cancelled`。固定并发键、自限定查询、状态前置条件及二次确认共同保证任务重复执行安全。命名 migration `20260808_064925_d5_payment_frontend_timeout` 纳入 workflow 枚举和轮询时间字段。
- 验证证据：新增 Native 二维码、H5 返回、服务端状态唯一判定、跨客户拒绝、原子限频、到期未支付关单、到期已支付不关单、未知状态不关单、关单失败保持待支付及任务重跑幂等测试。最终在同一数据库状态上连续两次原样 `make check`，两次均以退出码 0 完整通过生成物/schema 漂移、全部 migration 往返、Nginx、lint、TypeScript strict、556/556 单元测试、65/65 PostgreSQL/MinIO 集成测试、依赖与秘密门禁、Next.js 生产构建及 linux/amd64 同镜像；最终原样 `make test-e2e` 39/39 通过。
- 遗留风险/门槛：当前支付和关单仍只使用本地 fixture，`ALLOW_REAL_PROVIDER_WRITES=false`；没有接入真实微信商户号、没有真实资金请求、没有部署或修改共享 ECS。微信官方 fixture/沙箱联调、通知重放补单、特殊退款/发票备注审计和 D7 生产门槛仍需后续单独完成与授权。
- Git 状态：工作分支 `codex/d5-06-payment-frontend-timeout` 基于已合并 D5-05 的最新 `main`；本条随 D5-06 实现、migration、生成物、测试和文档提交推送，并创建指向 `main` 的 ready PR 交 Claude 审核。Codex 不批准、不合并、不部署；用户未跟踪的 `docs/planning/` 保持未修改且不进入提交。

### 2026-08-08（D5-07）

- 阶段状态：完成；开发计划 9.2 剩余的支付通知重放与补单、特殊退款/发票备注/人工操作审计、微信官方示例 fixture 自动化测试三项达到完成定义，D5 第 9.2 节实现项全部闭环。测试商户小额联调按项目负责人边界未执行，等待单独授权。
- 完成内容：新增受保护的 `paymentNotificationArchives`，D5-03 原通知处理只有在平台验签成功后才归档安全字段和 SHA-256 摘要；重放入口只允许有效 MFA `system_admin` 读取 `signatureVerified=true` 的归档，重新主动查单后复用既有金额/商户号/交易号核对、唯一约束、通知持久化和 `transitionOrder`，不接受原始未验签报文或人工支付结论，同一通知重复重放只迁移一次。订单补单入口同样仅依据服务端主动查单，人工只能提交处理备注与包含来源、引用、观察时间的结构化外部证据。新增 `orderManualActions` 和仅限 `system_admin` 的特殊退款/发票备注入口，统一追加操作者、订单、整数分金额、原因、外部证据和时间审计；特殊退款复用已确认支付记录，拒绝成功订单和非法金额，并在事务内锁定订单行，将 D5-04 自动退款占用与既有人工退款共同计入累计额度，关闭并发超额窗口，不调用 provider。新增命名 migration `20260808_074845_d5_payment_recovery_manual_audit`、Payload 生成类型和迁移 schema 门禁。微信测试以官方 API v3 通知示例的事件/资源结构为基线，在测试进程生成 RSA 平台密钥和 API v3 AES 密钥，覆盖 SHA256-RSA 验签、AES-256-GCM 解密及签名篡改拒绝；不保存官方文档未提供的私钥或不可验证静态密文。
- 验证证据：PostgreSQL/MinIO 集成测试模拟“验签归档后服务中断”，确认归档仍可由管理员重放、重放必做主动查单、首轮推进支付、再次重放不新增状态事件；补单测试确认支付结论仅来自 provider 查询；人工操作测试覆盖匿名拒绝、完整审计、成功订单禁退、并发人工退款串行化、自动退款额度占用和累计退款上限。migration verifier 覆盖新表字段、枚举、唯一索引、空库、历史升级和 D5-07 独立 down/up 往返。最终在同一数据库状态上连续两次原样 `make check`，两次均以退出码 0 完整通过生成物/schema 漂移、全部 migration 往返、Nginx、lint、TypeScript strict、557/557 单元测试、67/67 PostgreSQL/MinIO 集成测试、依赖与秘密门禁、Next.js 生产构建及 linux/amd64 同镜像。完整 E2E 前两次分别为 38/39、37/39，失败现场均伴随 Next 开发服务器 `ECONNRESET`；失败的 CMS 场景隔离复跑 1/1 通过，未发现产品回归，随后原样 `make test-e2e` 39/39 通过并作为最终证据。
- 遗留风险/门槛：`ALLOW_REAL_PROVIDER_WRITES=false`；未接真实微信商户号、未发送真实资金请求、未部署或修改共享 ECS。具备测试商户配置后的 Native/H5、退款及通知重放小额联调、平台证书/公钥轮换，以及 D7 的生产通知重放、重启、恢复和告警演练仍须单独授权并保持未勾选。特殊退款本片只记录受约束的人工财务处置证据，不新增绕过 D5-04 自动退款状态机的 provider 写路径。
- Git 状态：工作分支 `codex/d5-07-payment-recovery-audit-fixtures` 基于已合并 D5-06 的最新 `main`；本条随 D5-07 实现、migration、生成物、测试和文档提交推送，并创建指向 `main` 的 ready PR 交 Claude 审核。Codex 不批准、不合并、不部署；用户未跟踪的 `docs/planning/` 保持未修改且不进入提交。
