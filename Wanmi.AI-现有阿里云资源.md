# Wanmi.AI 现有阿里云资源清单

> 文档版本：v4.6（D7-06 KMS 不可用与应用主密钥版）
>
> 更新日期：2026-08-10
>
> 冻结基线：`P1-BASELINE-2026-08-10.1`；批准标签 `p1-docs-approved-2026-08-10-1`
>
> 适用范围：Wanmi.net 域名工具、内容与代理注册主站；Wanmi.ai 品牌跳转
>
> 交付基线：项目负责人一人 + ChatGPT/Codex；P1 全量一次交付，目标 12～16 周
>
> 批准边界：P1 开发已批准；生产上线附条件批准
>
> 关联文档：[技术栈与工程规范](./Wanmi.AI-技术栈.md) · [产品需求文档](./Wanmi.AI-产品需求文档-PRD.md)

## 1. 资源概览

| 资源 | 已知配置 | 当前状态 | P0/P1 用途 |
| --- | --- | --- | --- |
| ECS | 上海，2 vCPU、4 GiB、5 Mbps，1 台 | 已拥有；仍承载其他项目 | Nginx、Next.js/Payload Web、Payload Jobs Worker、Who-Dat |
| RDS PostgreSQL | PostgreSQL 16.10（实连确认），2 vCPU、4 GiB、100 GB，系列待确认 | 已拥有 | Payload 业务结构、用户、内容、订单、支付、域名资产、Session、Jobs 和审计 |
| OSS | 上海 D0 私有测试 Bucket 已创建；名称不入仓库 | 已拥有并完成 D0 兼容性验证 | 公共媒体经 Payload S3 Adapter；实名私有对象经 `ali-oss`；另含静态产物、快照和导出 |
| 短信服务 | 签名和模板待确认 | 已拥有 | 通过 Alibaba Cloud TypeScript SDK 提供手机验证码、订单通知和到期提醒 |
| 微信支付 | 公司商户号 | 已开通、可联调 | API v3 Native/H5 支付和原路退款 |
| 西部数码代理账号 | 账号已拥有，写接口权限待核验 | 部分可用 | 查询、价格、实名、注册、续费和 NS |
| KMS/Secrets Manager | D7-05 实测 `AccountStatus=NotEnabled` | 不提供、已从架构移除 | 无 P1 运行依赖；历史预检仅作为决策证据 |
| 实名应用主密钥 | 32 字节、版本化、部署 secret + 双人受控离线备份 | D7-06 实现完成；生产注入/轮换/恢复待演练 | 包裹每对象数据密钥，不写入仓库或镜像 |

P1 处理用户资金、实名资料、订单和域名资产，因此支付、私有证件存储、版本化应用主密钥信封加密、订单审计、三方对账和西部数码余额监控均为首发必需能力。P1 仍不保存证书私钥。

真实收款前必须完成 RDS 高可用与 PITR、OSS 加密与版本控制、密钥轮换、恢复演练、支付告警和支付通知重放验证；这些条件不阻塞开发与联调。

实例 ID、账号 ID、公网 IP、数据库地址和密钥不得写入本文。

## 2. ECS

### 2.1 已确认配置

| 项目 | 当前配置 |
| --- | --- |
| 地域 | 华东 2（上海） |
| CPU | 2 vCPU |
| 内存 | 4 GiB |
| 公网带宽 | 5 Mbps |
| 数量 | 1 台 |
| 定位 | P0/P1 单应用节点 |

### 2.2 部署内容

- Nginx：TLS、反向代理、安全头和基础限流；
- Next.js + Payload Web：工具站、内容站、运营后台、自定义 API 与 Payload Local/REST API；
- Payload Jobs Worker：注册、续费、退款、NS、对账、发布、通知和清理；
- Who-Dat：RDAP/WHOIS 查询；
- 云监控、日志和安全 Agent。

ECS 不保存唯一用户、订单、支付、域名、内容、广告、任务、证件、素材或主密钥唯一副本；业务数据必须进入 RDS/私有 OSS，镜像进入 ACR，应用主密钥从部署 secret 注入并有独立离线备份。

P1 不由 ECS 输出 Next.js 哈希静态文件、内容图片、广告素材或导出文件。它们通过 static.wanmi.net + OSS/CDN 提供。

### 2.3 容量边界

| 进程 | 建议内存上限 |
| --- | ---: |
| Nginx | 96 MiB |
| Next.js + Payload Web | D0 实测，初始预算 1.2 GiB |
| Payload Jobs Worker | D0 实测，初始预算 768 MiB |
| Who-Dat | 256 MiB |
| 系统、Docker、日志和监控 | 至少 1.2 GiB |

当前 2 核 4 GiB 适合开发、联调和低流量首发。固定 5 Mbps 暂时保留；支付通知必须可重放，订单可从 RDS 恢复，ECS 必须能在 2 小时内重建。上线后观察 7～14 天出流量、P95 峰值、commerce 队列和账单，再决定是否调整计费。

### 2.4 待核对

- [ ] ECS 规格族和规格 ID；
- [ ] 可用区；
- [ ] 操作系统和版本；
- [ ] 系统盘类型、容量和剩余空间；
- [ ] 公网计费方式；
- [ ] 公网 IPv4、到期时间和续费方式；
- [ ] 安全组只开放 80/443，22 限制管理来源；
- [ ] 自动快照和应用重建演练；
- [ ] 云助手、云安全中心和 CloudMonitor Agent；
- [ ] Docker 日志轮转和 2 GiB 紧急 swap。

## 3. RDS PostgreSQL

### 3.1 用途

- 内容、文章修订、分类、标签和 TLD 页面；
- 广告主、广告位、素材、活动和排期；
- 广告曝光/点击的必要事件与聚合；
- 管理员账号、Session、MFA 和权限；
- Payload Jobs 任务；
- 价格快照和 provider 元数据；
- 用户反馈和操作审计；
- 普通用户、Session、短信挑战和实名模板元数据；
- 报价、订单、支付通知、退款和对账；
- 域名资产、续费、Name Server 变更和 Payload commerce Jobs。

P1 不在 RDS 存储：

- 实名证件文件原文，文件只进入私有 OSS；
- 微信或西部数码密钥；
- 余额钱包或充值账户；
- SSL 私钥。

### 3.2 可用性结论

100 GB 对 P1 足够。实连确认为 PostgreSQL 16.10；仍需确认实例系列、存储类型和备份能力。

开发和联调可以使用当前实例。由于 P1 承接资金、实名、订单和域名资产，真实收款前必须确认或升级为满足高可用、自动备份、PITR 和恢复演练要求的生产实例。

D0 只在新建隔离数据库执行 Payload 初始 migration，首次应用成功、第二次执行无变更；现有业务数据库只读核对且未迁移、未改表。当前 RDS 未启用 SSL 且存在公网连接入口，生产使用前必须启用 SSL，并收敛为 VPC 内网访问和最小白名单；本次为避免影响现有项目未修改实例网络或 SSL 配置。

### 3.3 待核对

- [x] PostgreSQL 大版本和小版本（16.10）；
- [ ] 基础、高可用或集群系列；
- [ ] 实例规格代码和存储类型；
- [ ] 地域、可用区和 VPC；
- [ ] 最大连接数；
- [ ] 只允许 VPC 内网访问；
- [ ] 白名单只包含应用 ECS/安全组；
- [ ] 自动备份、日志备份和 PITR；
- [ ] 备份保留天数；
- [ ] 恢复演练结果；
- [ ] 慢 SQL、CPU、连接、IOPS 和存储告警；
- [ ] 实例到期、自动续费和欠费提醒；
- [ ] Payload Web 与 Jobs Worker 连接池总量不超过 RDS 安全连接预算；
- [x] Payload PostgreSQL Adapter 在 D0 隔离数据库完成 migration、事务路径和 `push: false` 验证；
- [ ] 生产数据库升级快照、连接池预算和正式迁移演练通过。

## 4. OSS 与 CDN

### 4.1 数据分区

| 前缀/类别 | 内容 | 访问 |
| --- | --- | --- |
| public/static | Next.js 哈希静态产物 | CDN 公共读 |
| public/content | 文章、TLD 和专题图片 | CDN 公共读 |
| public/ads | 已审核广告图片 | CDN 公共读 |
| private/snapshots | provider 原始快照 | 仅应用和受权管理员 |
| private/exports | 运营临时导出 | 私有、短生命周期 |
| private/realname | 每对象数据密钥 + 版本化应用主密钥信封加密的用户实名证件 | 仅受控后端和最小权限流程 |

实名证件保存至用户删除模板或注销账号，随后 30 天内清理主存储和备份。P1 不创建用户证书或私钥存储区。

### 4.2 广告素材要求

- 素材由运营上传到受控前缀；
- 不允许广告主提供运行时 HTML/JavaScript；
- 图片进行格式、大小和安全检查；
- 发布后使用 Wanmi OSS/CDN 地址；
- 下线和删除保留必要审计；
- 广告和内容素材使用不同路径与缓存规则。

### 4.3 待核对

- [x] D0 Bucket 地域为上海且名称不写入仓库；
- [x] 是否与 ECS/RDS 同为上海；
- [x] D0 Bucket 公共访问阻止；
- [ ] 服务端加密；
- [ ] 版本控制和生命周期；
- [ ] CORS、Referer 和 CDN 回源；
- [ ] static.wanmi.net 证书与缓存；
- [ ] 静态文件先上传后发布及回滚；
- [ ] RAM Role 最小权限；
- [ ] 私有快照和导出访问审计；
- [ ] private/realname 独立前缀、应用主密钥信封加密、恶意文件检查和访问审计；
- [ ] 实名模板删除、账号注销和 30 天清理任务；
- [ ] 存储量、流量和异常请求告警。

### 4.4 公共媒体与实名文件接入边界

Payload 公共 Upload Collection 通过 `@payloadcms/storage-s3` 接入阿里云 OSS。由于 OSS 只兼容部分 S3 API且要求 virtual-hosted style，D0 必须在目标 Bucket 实测上传、读取、删除、短时签名地址和 ETag；不得仅以 AWS S3 本地 mock 结果判定可用。

实名证件不复用公共 Media Collection，也不经过公共 `@payloadcms/storage-s3` 配置。它使用独立私有 Collection、`private/realname` 前缀和单独 RAM 权限，通过 `ali-oss` 完成对象上传、读取、短时签名和删除；应用为每个对象生成独立数据密钥，以版本化应用主密钥包裹。项目不得自行实现 OSS 或短信的阿里云请求签名。

2026-08-04 D0 实测已在专用私有 Bucket 完成：Payload `storage-s3` 公共 Media 路径和独立 `ali-oss` 私有路径均通过上传、读取、ETag、60 秒签名地址、删除及删除后不存在验证，测试对象已清理。2026-08-10 根据 KMS `NotEnabled` 事实移除 KMS。RAM 最小权限、生产应用主密钥注入/轮换/紧急恢复、版本控制、生命周期与误删恢复仍是生产前待办。

## 5. 短信服务

P1 使用短信完成手机验证码登录、注册/退款重要状态通知和域名到期提醒，因此短信服务是 P1 上线阻塞项。

应用通过 Alibaba Cloud TypeScript SDK 调用短信服务；业务层只依赖 `aliyunsms` adapter，SDK 原始响应必须先映射为稳定的领域错误，且日志不得包含完整手机号、验证码或访问凭据。

进入生产联调前确认：

- 国内短信资质；
- 签名和模板；
- 余额和发送上限；
- 单手机号、IP 和全局频率；
- 回执、失败和告警；
- RAM Role 与最小权限。

管理员 P1 默认使用密码 + TOTP MFA，不以短信作为唯一二次验证。普通用户验证码登录与管理员认证完全分离。

## 6. P1 需开通或确认的资源

以下能力尚未被本文确认为已经拥有或可投入生产：

- ACR 私有镜像仓库；
- CDN 与 static.wanmi.net；
- 云解析 DNS 和 HTTPS 证书；
- SLS；
- CloudMonitor 和外部 URL 探测；
- 部署 secret、支付/provider 凭据及实名应用主密钥轮换；
- RAM Role；
- 云安全中心；
- Wanmi.ai 非中国内地 HTTPS 跳转能力；
- Wanmi.net ICP 备案与公安联网备案；
- 西部数码实名、注册、续费、Name Server 写接口书面权限；
- 西部数码作为域名注册服务机构及 Wanmi 代理关系、责任边界、售后和页面披露方式的专业确认；
- 西部数码代理余额充值、阈值告警和紧急停售；
- 微信支付平台证书、API v3 密钥、Native/H5 产品配置和退款权限。

P1 默认不采购以下资源；触发第 10 节扩容条件时重新评估：

- Tair/Redis；
- 独立消息队列；
- 第二台 ECS；
- ALB；
- WAF；
- ACK；
- OpenSearch；
- 支付宝和微信 JSAPI 支付；
- 余额钱包；
- 移动推送；
- 第三方用户身份服务。

## 7. 当前可用性判断

| 判断 | 结论 |
| --- | --- |
| 是否可以开始 P1 开发 | 可以，外部权限和备案与开发并行确认 |
| ECS 是否够用 | 适合开发、联调和低流量首发，需严格资源限制与恢复能力 |
| RDS 是否够用 | 容量足够，版本、系列和备份待确认 |
| 是否必须立即升级高可用 RDS | 开发不阻塞；真实收款前必须满足高可用、PITR 和恢复要求 |
| OSS 是否可用 | 可以，先完成地域、权限、加密和生命周期核对 |
| 短信是否是 P1 必需 | 是，用于手机验证码和到期提醒 |
| 微信支付是否可用 | 公司商户号已开通，可进入 Native/H5 联调 |
| 西部数码写接口是否可用 | 代理账号已拥有，实名/注册/续费/NS 权限待核验 |
| 域名代理合规是否确认 | 待外部专业人员复核代理资质、责任边界和页面显著披露方式；不阻塞开发，阻塞真实收款上线 |
| ICP/公安联网备案是否完成 | 待办理或核验，真实收款上线前必须完成 |
| 是否具备应用高可用 | 不具备，单 ECS 是单点 |
| 是否立即购买第二台 ECS | 不需要，按流量和可用性阈值扩容 |

## 8. 监控与成本

### 8.1 必须监控

- ECS CPU、内存、磁盘、带宽、swap 和容器重启；
- RDS CPU、连接、慢 SQL、IOPS、存储和备份；
- OSS/CDN 请求量、命中率、流量和错误；
- 工具请求量、成功率、P95 和 provider 错误；
- 西部数码调用量、429 和拒绝；
- DNS/TLS 安全拒绝；
- 广告图片、目标链接和点击异常；
- 短信请求、成功率、限频、回执和余额；
- 微信支付通知、主动查单、退款、签名失败和证书到期；
- 订单状态、履约耗时、退款失败和人工处理积压；
- 西部数码写接口、账户余额、注册/续费成功率和异常状态；
- 实名证件访问、应用主密钥配置、删除任务和异常下载；
- 微信、西部数码和内部订单三方对账差异；
- 证书、域名、资源包和实例到期。

### 8.2 成本归因

至少按月记录：

- ECS、RDS、OSS、CDN、SLS 和部署 secret/离线主密钥备份保管；
- provider 或数据源成本；
- 内容图片和广告素材流量；
- 每千次有效工具请求成本；
- 每千页面浏览基础设施成本；
- 广告与导购收入；
- 域名注册/续费实收、上游成本、支付成本和服务毛差；
- 微信退款和西部数码预充值余额；
- 内容与广告运营人工成本。

资源成本必须能与页面 RPM、工具使用量、域名服务毛差和广告收入对照。

## 9. 上线前优先级

### P0：立即核对

1. 确认 PostgreSQL 大版本、系列和备份；
2. 确认 ECS 操作系统、磁盘、安全组和到期；
3. 确认 OSS Bucket、公共访问和加密；
4. 确认 CDN、ACR、SLS、CloudMonitor、部署 secret 和主密钥离线备份状态；
5. 核对资源账号、地域和 VPC；
6. 从上海 ECS 连续测试 RDAP/WHOIS、西部数码、DNS 和 TLS；
7. 验证 Wanmi.ai 跳转和 Wanmi.net 备案条件；
8. 核验西部数码实名、注册、续费、NS 写接口和代理余额；
9. 核验微信支付 API v3 Native/H5、退款和平台证书；
10. 核验短信签名、模板、余额、回执和限频。
11. 由外部专业人员复核域名代理资质、双方责任、售后边界、真实身份核验、个人信息保护和页面显著披露方式。

### P1：部署前

1. 配置 static.wanmi.net、CDN 和静态回滚；
2. 建立内容与广告素材分区；
3. 建立 RAM Role 与最小权限；
4. 配置日志、监控、告警、欠费和到期提醒；
5. 完成 ECS 重建、RDS 恢复和 OSS 误删恢复演练；
6. 将 DNS、证书、镜像和生产配置纳入发布流程；
7. 记录资源负责人、续费日期和故障联系方式；
8. 验证广告关闭时网站和工具完整可用；
9. 完成私有实名 OSS、应用主密钥信封加密、访问审计和 30 天删除；
10. 完成支付通知重放、订单恢复、自动退款和三方对账；
11. 完成 ICP 与公安联网备案以及隐私、实名、支付条款审核；
12. 完成异常订单、退款失败、余额不足和实名泄露 Runbook。
13. 验证站点显著标明所代理的域名注册服务机构，并与已审核的协议、隐私和售后文案一致。

## 10. 扩容条件

满足任一条件后评估扩容：

- CPU 持续 15 分钟超过 65%；
- 内存持续超过 75%、OOM 或持续 swap；
- 出带宽持续超过 70%；
- 排除外部数据源后核心 API P95 超过 800 ms；
- 后台任务积压超过 10 分钟；
- 页面流量增长导致静态资源或日志成本异常；
- 稳定商业收入后不能接受单 ECS 故障；
- 真实订单增长后单 ECS 停机持续影响支付、履约或客服；
- commerce 队列或人工处理持续积压。

容量不足优先升级到 4 核 8 GiB；需要应用高可用时增加第二台 ECS + ALB。多实例前必须处理共享限流、用户与管理员 Session、缓存、commerce worker 选主和定时任务去重。

## 11. 资源变更记录

| 日期 | 变更 |
| --- | --- |
| 2026-07-30 | 建立 ECS、RDS、OSS 和短信资源清单 |
| 2026-07-31 | 原双后端交易技术方案，现已废止 |
| 2026-08-02 | 早期资源草案由 v4.0 Payload 主架构资源版取代；v4.0 后续由当前 v4.1 细化 |
| 2026-08-02 | P1 恢复代理注册交易；微信支付已可联调，西部数码写权限与备案待核验 |
| 2026-08-02 | 实名证件进入私有 OSS + KMS；真实收款前恢复 RDS 高可用和恢复硬门槛 |
| 2026-08-02 | 主进程调整为 Next.js/Payload Web + 独立 Payload Jobs Worker；增加 OSS S3 兼容和单 ECS 资源验证 |
| 2026-08-03 | 公共媒体继续使用 Payload S3 Adapter；实名私有对象固定使用 ali-oss；短信与 KMS 固定使用 Alibaba Cloud TypeScript SDK，不新增云资源 |
| 2026-08-03 | v4.2 冻结 P1 资源与上线核验基线；增加域名代理资质、责任边界和页面披露专业复核门槛 |
| 2026-08-03 | v4.3 批准现有 OSS/RDS/ECS 的 D0 受控验证；ECS 仍运行其他项目，迁出前仅允许不影响现有服务的只读盘点，禁止在共享节点执行 Wanmi 压测、现有服务重启、空节点重建或 RTO 演练 |
| 2026-08-04 | v4.4 完成真实 OSS 两条 adapter 路径与隔离 RDS migration 验证；现有业务数据库未改动，ECS 只做盘点和临时加密端口转发；记录 RDS SSL/公网风险并延期同 VPC Worker、ECS 压测、重启、重建与 RTO 验证 |
| 2026-08-04 | v4.5 项目负责人批准 D0 条件通过；共享 ECS 的资源、独立重启、同 VPC Jobs 恢复、节点重建和 RTO 验证转入 D7，现有项目迁出前禁止在该节点部署或压测 Wanmi |
| 2026-08-10 | v4.6：D7-05 实测 KMS `AccountStatus=NotEnabled` 后按负责人指示移除 KMS；改为版本化应用主密钥包裹每对象数据密钥，生产注入、双人受控离线备份、轮换和紧急恢复继续作为硬门槛 |
